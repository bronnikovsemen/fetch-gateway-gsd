---
phase: 02-pre-provider-flow
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/app/permissions/page.tsx
autonomous: true
requirements:
  - FLOW-03
tags: [mvp, vertical-slice, permissions, grid, mui-button, client-component, flow-layout-consumer]

must_haves:
  truths:
    - "Visiting `/permissions` shows a centered 768px-wide white panel on the `#EBF5FF` page background — no Phase 1 placeholder heading, no muted hint"
    - "The white panel has 48px vertical padding and 36px horizontal padding (via FlowLayout's new px={4.5} py={6} from Plan 02-01)"
    - "Inside the panel, the user sees the Fetch logo, the heading 'To connect your payroll, Fetch will need access to:', and a 2-column grid of exactly six PermissionItem rows"
    - "The six PermissionItem rows render in spec order: left column = Organization / Team / Employment; right column = Payroll / Pay Statement / SSN — each with the spec's bold label and muted description text"
    - "Two buttons appear side by side at the bottom: 'Back' (outlined) navigates to `/welcome`, 'Continue' (primary contained) navigates to `/select-provider` — both are real client-side navigations"
  artifacts:
    - path: "src/app/permissions/page.tsx"
      provides: "/permissions real screen — FetchLogo + heading + 2x3 PermissionItem grid + Back/Continue buttons"
      contains: "'use client'"
      contains_also: "To connect your payroll, Fetch will need access to:"
      contains_also: "Organization"
      contains_also: "Team"
      contains_also: "Employment"
      contains_also: "Payroll"
      contains_also: "Pay Statement"
      contains_also: "SSN"
  key_links:
    - from: "src/app/permissions/page.tsx (Back button)"
      to: "next/navigation router"
      via: "<Button variant='outlined' onClick={() => router.push('/welcome')}>"
      pattern: "router\\.push\\(['\"]\\/welcome['\"]\\)"
    - from: "src/app/permissions/page.tsx (Continue button)"
      to: "next/navigation router"
      via: "<Button variant='contained' onClick={() => router.push('/select-provider')}>"
      pattern: "router\\.push\\(['\"]\\/select-provider['\"]\\)"
    - from: "src/app/permissions/page.tsx"
      to: "src/components/PermissionItem.tsx"
      via: "six <PermissionItem label=... description=... /> renders inside a 2-column Box grid"
      pattern: "<PermissionItem"
---

<objective>
Replace the Phase 1 `/permissions` placeholder (which renders only one sample PermissionItem as a smoke test) with the real permissions disclosure screen: a centered 768px-wide white panel on `#EBF5FF` with 48px vertical / 36px horizontal padding, the Fetch logo, the heading "To connect your payroll, Fetch will need access to:", a 2-column grid of all six permission scopes (Organization, Team, Employment, Payroll, Pay Statement, SSN) with the spec's exact descriptions, and two side-by-side buttons — "Back" (outlined → `/welcome`) and "Continue" (primary → `/select-provider`).

Purpose:
- Delivers FLOW-03 in a single vertical slice — the user reads exactly what Fetch will access, and has both forward and back navigation.
- Closes the Phase 2 trust narrative: the user arrives via the splash auto-redirect (Plan 02-02) → reads the welcome (Plan 02-03) → sees the permission disclosure (this plan) → continues into the Phase 3 provider-selection flow.
- Second real consumer of Plan 02-01's new FlowLayout px/py API, exercising the SPLIT shape (px=4.5 / py=6 = 36px / 48px) — the exact shape that motivated WR-02 in the Phase 1 REVIEW.

Output:
- `src/app/permissions/page.tsx` rewritten as a Client Component with the full 2-column grid + dual navigation.
- No new components, no new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./Main_Fetch_Gateway.md
@.planning/phases/01-foundation-shared-chrome/01-02-SUMMARY.md
@.planning/phases/01-foundation-shared-chrome/01-03-SUMMARY.md
@.planning/phases/02-pre-provider-flow/02-01-PLAN.md

<interfaces>
<!-- Components available (Phase 1 + Plan 02-01) -->

From src/components/FlowLayout.tsx (after Plan 02-01):
- type FlowLayoutProps = { children: ReactNode; maxWidth?: number; px?: number; py?: number }
- defaults: maxWidth=440, px=6 (48px), py=6 (48px)

From src/components/FetchLogo.tsx:
- type FetchLogoProps = { size?: number; color?: string; title?: string }
- defaults: size=100, color='#2463EC'

From src/components/PermissionItem.tsx:
- type PermissionItemProps = { label: string; description: string }
- renders a Stack(row): CheckCircleIcon (primary.main) + bold label + muted description

<!-- Permission scope catalog (from Main_Fetch_Gateway.md, /permissions section, EXACT spec strings):

| Label         | Description                                                       |
|---------------|-------------------------------------------------------------------|
| Organization  | Business profile, contact details, and banking information       |
| Team          | Roster of people and reporting structure                         |
| Employment    | Employment status, contact details, role, and compensation       |
| Payroll       | Payments made to employees and contractors                       |
| Pay Statement | Itemized pay statements per employee                             |
| SSN           | Social Security Numbers for tax reporting                        |

Spec layout (Main_Fetch_Gateway.md): 3 per column.
  Left column:  Organization / Team / Employment
  Right column: Payroll / Pay Statement / SSN

Note: No trailing periods on any description string — copy verbatim.
-->

<!-- Next.js 15 App Router client routing: useRouter() from 'next/navigation', then router.push('/welcome') / router.push('/select-provider'). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite /permissions as the real disclosure screen with 2x3 permission grid and Back/Continue navigation</name>
  <files>src/app/permissions/page.tsx</files>
  <read_first>
    - src/app/permissions/page.tsx (current Phase 1 stub — being fully replaced; note it already imports PermissionItem)
    - Main_Fetch_Gateway.md (spec section `### /permissions — Permissions` lines ~78-96 — the heading, the 6 permission items + descriptions, and Back/Continue button copy are all sourced from here verbatim)
    - .planning/ROADMAP.md (Phase 2 Success Criteria 3 and 4 — defines externally observable behavior)
    - .planning/phases/02-pre-provider-flow/02-01-PLAN.md (FlowLayout's new px/py API — this plan exercises the SPLIT shape, the very motivation for WR-02 in the Phase 1 REVIEW)
    - src/components/PermissionItem.tsx (confirms props are `{ label: string; description: string }` and renders Stack(row) with CheckCircleIcon + bold label + muted description)
    - src/components/FlowLayout.tsx (post Plan 02-01 — confirms props are maxWidth/px/py)
    - src/theme/theme.ts (theme.palette.primary.main = #2463EC for Continue button color; text.primary / text.secondary tokens for typography)
  </read_first>
  <action>
    Fully overwrite `src/app/permissions/page.tsx`. Build it following this directive prose — do not embed code blocks in the source comments; the description below names every identifier, prop, value, and string the executor must encode.

    Step 1 — Client directive. First non-blank line of the file is the literal `'use client'` directive. This is required because the Back and Continue buttons use `onClick` handlers that call `router.push`.

    Step 2 — Imports (exact set, per-module imports to match the repo style used by Phase 1):
      - Named import `useRouter` from `'next/navigation'`.
      - Default import `Box` from `'@mui/material/Box'`.
      - Default import `Stack` from `'@mui/material/Stack'`.
      - Default import `Typography` from `'@mui/material/Typography'`.
      - Default import `Button` from `'@mui/material/Button'`.
      - Default import `FlowLayout` from `'@/components/FlowLayout'`.
      - Default import `FetchLogo` from `'@/components/FetchLogo'`.
      - Default import `PermissionItem` from `'@/components/PermissionItem'`.

    Step 3 — At module scope (above the `Page` function), declare a `const PERMISSIONS` array. Type it as `readonly { label: string; description: string }[]` by appending `as const satisfies readonly { label: string; description: string }[]` to the literal, matching the pattern Phase 1 used in `src/lib/providers.ts`. Populate the array with these six entries in this EXACT order (matching the spec table — no rewording, no period changes; descriptions have NO trailing period):
      1. label `Organization`, description `Business profile, contact details, and banking information`
      2. label `Team`, description `Roster of people and reporting structure`
      3. label `Employment`, description `Employment status, contact details, role, and compensation`
      4. label `Payroll`, description `Payments made to employees and contractors`
      5. label `Pay Statement`, description `Itemized pay statements per employee`
      6. label `SSN`, description `Social Security Numbers for tax reporting`

    Step 4 — The default-exported `Page` component:

      4a. At the top of the function body, call `const router = useRouter();`.

      4b. Return a single `FlowLayout` element with these props: `maxWidth={768}`, `px={4.5}`, `py={6}`. This produces the spec's 768px-wide panel with 36px horizontal padding (4.5 × 8 = 36) and 48px vertical padding (6 × 8 = 48). This is the FIRST consumer to exercise the asymmetric px/py shape introduced by Plan 02-01.

      4c. Inside FlowLayout, render a single outer `Stack` with `spacing={4}` and `sx={{ alignItems: 'stretch' }}`. Stretch alignment (not center) is required because the grid child below wants to fill the panel width — center would collapse it to content width.

      4d. Inside that outer Stack, in this exact order, three children:

        i. A nested centered `Stack` with `spacing={3}` and `sx={{ alignItems: 'center' }}`. Inside this header Stack, render two children:
           - A `FetchLogo` element with `size={100}`.
           - A `Typography` element with `variant="h5"`, `component="h1"`, and `sx={{ fontWeight: 700, color: 'text.primary', textAlign: 'center' }}`. Its text content is the EXACT spec heading: `To connect your payroll, Fetch will need access to:` (note: comma after "payroll", colon at the end — match verbatim).

        ii. A `Box` element that renders the 2-column grid. Its `sx` prop must be an object with the following entries (in any order):
           - `display: 'grid'`
           - `gridTemplateColumns: '1fr 1fr'`
           - `gridTemplateRows: 'repeat(3, auto)'`
           - `gridAutoFlow: 'column'`
           - `columnGap: 4` (theme spacing → 32px)
           - `rowGap: 3` (theme spacing → 24px)

           The `gridAutoFlow: 'column'` combined with three explicit rows is the mechanism that produces the spec's column-major fill (left column = items 1-3 = Organization/Team/Employment; right column = items 4-6 = Payroll/Pay Statement/SSN) while letting the source code iterate the array in natural spec order. Add a one-line inline JS comment immediately above this `Box` saying: `gridAutoFlow: 'column' so the 6 items column-fill into left=Org/Team/Employment, right=Payroll/Pay Statement/SSN per spec`.

           Inside the Box, map over `PERMISSIONS` with `.map((perm) => ( <PermissionItem key={perm.label} label={perm.label} description={perm.description} /> ))`. Use `perm.label` for the React `key` (each label is unique).

        iii. A button-row `Stack` with `direction="row"`, `spacing={2}`, and `sx={{ justifyContent: 'flex-end', mt: 2 }}`. Render two `Button` children inside in this exact order:
           - Back button: `variant="outlined"`, `color="primary"`, `size="large"`, `onClick={() => router.push('/welcome')}`, `sx={{ textTransform: 'none', fontWeight: 600, minWidth: 120 }}`, text content `Back`.
           - Continue button: `variant="contained"`, `color="primary"`, `size="large"`, `onClick={() => router.push('/select-provider')}`, `sx={{ textTransform: 'none', fontWeight: 600, minWidth: 160 }}`, text content `Continue`.

    Step 5 — Constraints to honor while writing:
      - No `console.log`. No `: any`. No hardcoded brand hex codes — the buttons' brand-blue fill must come from `color="primary"` reading the theme; text colors must come from `text.primary` / `text.secondary` tokens.
      - No `Link` from `next/link` — use `router.push` like Plan 02-03 for consistent imperative navigation across the flow.
      - `textTransform: 'none'` is required on both buttons because MUI's default uppercases button text and the spec uses sentence case ("Back", "Continue", not "BACK", "CONTINUE").
      - The Phase 1 placeholder strings `Permissions (\`/permissions\`)` and `Phase 1 placeholder — real permissions grid lands in Phase 2 (FLOW-03).` and the one-off smoke-test `<PermissionItem label="Organization" description="Business profile, contact details, and banking information." />` (note: the smoke-test version had a trailing period — drop it; the spec strings have NO trailing period) must all be removed from the file.
      - Six PermissionItem renders must appear in the output, one per spec scope.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File starts with `'use client'` directive on the first non-blank line.
      - check: `head -3 src/app/permissions/page.tsx | grep -c "'use client'"` returns 1
    - The exact heading string appears verbatim, with comma and trailing colon.
      - check: `grep -cF "To connect your payroll, Fetch will need access to:" src/app/permissions/page.tsx` returns 1
    - All six permission labels appear as strings in the file.
      - check: `grep -cF "'Organization'" src/app/permissions/page.tsx` returns at least 1
      - check: `grep -cF "'Team'" src/app/permissions/page.tsx` returns at least 1
      - check: `grep -cF "'Employment'" src/app/permissions/page.tsx` returns at least 1
      - check: `grep -cF "'Payroll'" src/app/permissions/page.tsx` returns at least 1
      - check: `grep -cF "'Pay Statement'" src/app/permissions/page.tsx` returns at least 1
      - check: `grep -cF "'SSN'" src/app/permissions/page.tsx` returns at least 1
    - All six description strings appear verbatim (no trailing periods).
      - check: `grep -cF "Business profile, contact details, and banking information" src/app/permissions/page.tsx` returns 1
      - check: `grep -cF "Roster of people and reporting structure" src/app/permissions/page.tsx` returns 1
      - check: `grep -cF "Employment status, contact details, role, and compensation" src/app/permissions/page.tsx` returns 1
      - check: `grep -cF "Payments made to employees and contractors" src/app/permissions/page.tsx` returns 1
      - check: `grep -cF "Itemized pay statements per employee" src/app/permissions/page.tsx` returns 1
      - check: `grep -cF "Social Security Numbers for tax reporting" src/app/permissions/page.tsx` returns 1
    - None of the descriptions end with a period (smoke-test artefact removed).
      - check: `grep -cF "Business profile, contact details, and banking information." src/app/permissions/page.tsx` returns 0
    - The Back button navigates to /welcome and the Continue button navigates to /select-provider.
      - check: `grep -cE "router\\.push\\(['\"]\\/welcome['\"]\\)" src/app/permissions/page.tsx` returns 1
      - check: `grep -cE "router\\.push\\(['\"]\\/select-provider['\"]\\)" src/app/permissions/page.tsx` returns 1
    - The button labels are exactly Back and Continue.
      - check: `grep -cF ">Back<" src/app/permissions/page.tsx` returns 1
      - check: `grep -cF ">Continue<" src/app/permissions/page.tsx` returns 1
    - The Back button is variant="outlined" and the Continue button is variant="contained".
      - check: `grep -cE "variant=['\"]outlined['\"]" src/app/permissions/page.tsx` returns at least 1
      - check: `grep -cE "variant=['\"]contained['\"]" src/app/permissions/page.tsx` returns at least 1
    - FlowLayout is used with the spec-mandated 768px width and split px/py shape.
      - check: `grep -cE "<FlowLayout[^>]*maxWidth=\\{768\\}" src/app/permissions/page.tsx` returns 1
      - check: `grep -cE "px=\\{4\\.5\\}" src/app/permissions/page.tsx` returns 1
      - check: `grep -cE "py=\\{6\\}" src/app/permissions/page.tsx` returns 1
    - The grid uses gridAutoFlow column so the source-order array column-fills per spec.
      - check: `grep -cE "gridAutoFlow:\\s*['\"]column['\"]" src/app/permissions/page.tsx` returns 1
      - check: `grep -cE "gridTemplateRows:\\s*['\"]repeat\\(3,\\s*auto\\)['\"]" src/app/permissions/page.tsx` returns 1
      - check: `grep -cE "gridTemplateColumns:\\s*['\"]1fr 1fr['\"]" src/app/permissions/page.tsx` returns 1
    - Exactly six PermissionItem renders exist (one per scope; iteration produces six elements at runtime).
      - check: `grep -cE "<PermissionItem\\b" src/app/permissions/page.tsx` returns at least 1 (single instantiation inside the map)
      - check: SSR markup contains six CheckCircleIcon SVG paths after live smoke (see below)
    - No raw hex brand colors leaked into the file.
      - check: `grep -cF "#2463EC" src/app/permissions/page.tsx` returns 0
      - check: `grep -cF "#EBF5FF" src/app/permissions/page.tsx` returns 0
    - The Phase 1 placeholder strings are gone.
      - check: `grep -cF "Permissions (\`/permissions\`)" src/app/permissions/page.tsx` returns 0
      - check: `grep -cF "Phase 1 placeholder" src/app/permissions/page.tsx` returns 0
    - TypeScript compiles with no errors.
      - check: `npx tsc --noEmit` exits 0
    - Hygiene gates from CLAUDE.md hold for this file.
      - check: `grep -cE "console\\.log" src/app/permissions/page.tsx` returns 0
      - check: `grep -vE "^\\s*//" src/app/permissions/page.tsx | grep -cE "(^|[^a-zA-Z_]): *any( |;|,|>|$)"` returns 0
    - Live HTTP smoke: with `npm run dev` running on 3001, `curl -s http://localhost:3001/permissions` returns 200 and the body contains the heading, all six labels, all six descriptions, and both button labels.
      - check: `curl -s -o /tmp/permissions.html -w "%{http_code}" http://localhost:3001/permissions` returns 200
      - check: `grep -cF "To connect your payroll, Fetch will need access to:" /tmp/permissions.html` returns at least 1
      - check: `grep -cF "Organization" /tmp/permissions.html` returns at least 1
      - check: `grep -cF "Pay Statement" /tmp/permissions.html` returns at least 1
      - check: `grep -cF "SSN" /tmp/permissions.html` returns at least 1
      - check: `grep -cF "Back" /tmp/permissions.html` returns at least 1
      - check: `grep -cF "Continue" /tmp/permissions.html` returns at least 1
  </acceptance_criteria>
  <done>
    `/permissions` renders the 768px white panel on `#EBF5FF` with 36px horizontal / 48px vertical padding, the heading, a 2-column grid of all six permission scopes (column-major fill — left=Org/Team/Employment, right=Payroll/Pay Statement/SSN), and Back (outlined) + Continue (primary) buttons that navigate to `/welcome` and `/select-provider` respectively. `tsc --noEmit` exits 0 and `curl /permissions` returns 200 with the spec copy in the SSR markup.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Static content with two client-side navigations; no user input, no network calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | router.push targets | accept | Both targets (`/welcome`, `/select-provider`) are static literals — no untrusted input. The two buttons are single-purpose navigation controls. |
| T-02-04-02 | Information disclosure | Permission disclosure copy | accept | The spec ITSELF mandates the disclosure copy — telling the user what data will be read. This is by design, not a leak. |
</threat_model>

<verification>
- `npx tsc --noEmit` → exit 0.
- All grep gates in `<acceptance_criteria>` pass (35+ checks covering: directive, heading, six labels, six descriptions, both nav targets, both button labels, both variants, FlowLayout 768px + px/py shape, grid CSS, no hex leakage, no placeholder strings, no console/any).
- Live `curl http://localhost:3001/permissions` returns 200 with the heading + all six scope labels + both button labels in the SSR markup.
- ROADMAP Phase 2 Success Criteria 3 and 4 observable in a browser: 768px panel, 2-column grid in spec order, Back navigates to `/welcome`, Continue navigates to `/select-provider`.
</verification>

<success_criteria>
- FLOW-03 satisfied: centered 768px white panel, 36px horizontal / 48px vertical padding, FetchLogo + heading + 2x3 grid of six PermissionItems (column-major spec order) + Back (outlined) + Continue (primary) buttons.
- Back navigates to `/welcome` (real route, no dead button).
- Continue navigates to `/select-provider` (real route, no dead button).
- All six spec scope labels and all six spec descriptions present verbatim, no trailing periods.
- ROADMAP Phase 2 Success Criteria 3 and 4 observable end-to-end.
</success_criteria>

<output>
Create `.planning/phases/02-pre-provider-flow/02-04-SUMMARY.md` when done. Include:
- One-line summary of the permissions screen rebuild.
- Confirmation of FLOW-03 requirement satisfaction and that this plan is the second consumer of FlowLayout's new px/py API — exercising the SPLIT shape (`px={4.5} py={6}`) that motivated WR-02 in the Phase 1 REVIEW.
- Verification evidence: `tsc --noEmit` exit code, all the greps from `<acceptance_criteria>`, and the live HTTP smoke result showing all six scope labels in the SSR body.
- Confirmation that the Phase 1 placeholder strings and the one-off smoke-test PermissionItem are gone from `src/app/permissions/page.tsx`.
</output>
