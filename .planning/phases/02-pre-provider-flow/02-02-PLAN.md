---
phase: 02-pre-provider-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/app/page.tsx
autonomous: true
requirements:
  - FLOW-01
tags: [mvp, vertical-slice, splash, animation, auto-redirect, client-component]

must_haves:
  truths:
    - "Visiting `/` on the dev server (port 3001) renders the Fetch logo centered on the `#EBF5FF` page background with the tagline 'Retirement runs on Fetch' below it — no placeholder heading, no 'Phase 1 placeholder' muted hint"
    - "The Fetch logo on `/` plays a 500ms scale-in animation on first mount (from a smaller scale to its final 100px size)"
    - "After the scale-in completes, the logo enters a 2-second breathing pulse cycle (subtle scale oscillation) until the auto-redirect fires"
    - "After approximately 2500ms from mount, the browser navigates from `/` to `/welcome` via Next.js client-side routing (no full page reload, no anchor click, no manual user action)"
    - "The auto-redirect is cancelled if the component unmounts before the timeout completes (no orphaned setTimeout firing on stale state)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "/ Splash screen — Client Component with FetchLogo, tagline, scale-in + breathing animations, useEffect-driven setTimeout to /welcome"
      contains: "'use client'"
      contains_also: "Retirement runs on Fetch"
      contains_also: "router.push"
      contains_also: "/welcome"
      contains_also: "setTimeout"
      contains_also: "2500"
  key_links:
    - from: "src/app/page.tsx"
      to: "next/navigation router"
      via: "useRouter().push('/welcome') inside a setTimeout"
      pattern: "router\\.push\\(['\"]/welcome['\"]\\)"
    - from: "src/app/page.tsx"
      to: "src/components/FetchLogo.tsx"
      via: "<FetchLogo size={100} /> rendered inside an animated wrapper Box"
      pattern: "<FetchLogo"
---

<objective>
Replace the Phase 1 `/` placeholder (FetchLogo + "Splash (`/`)" heading + muted-hint paragraph) with the real splash screen: centered Fetch logo on the `#EBF5FF` page background, "Retirement runs on Fetch" tagline, 500ms scale-in animation followed by a 2s breathing pulse, and a client-side auto-redirect to `/welcome` after ~2500ms.

Purpose:
- Delivers FLOW-01 in a single vertical slice — the user lands on `/`, sees the on-brand splash for ~2.5s, and is carried into `/welcome` automatically.
- Establishes the canonical client-component pattern (`'use client'` + `useRouter` + `useEffect` + cleanup) that Phase 3's `/connecting` auto-advance will mirror.

Output:
- `src/app/page.tsx` rewritten as a Client Component with the splash UI + animation + auto-redirect.
- No new packages, no new components — animations use MUI `Box` `sx` + Emotion `@keyframes` (which is already the Emotion in MUI's dependency chain), or `keyframes` from `@emotion/react` (already a peer dep — see `package.json`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./Main_Fetch_Gateway.md
@.planning/phases/01-foundation-shared-chrome/01-02-SUMMARY.md
@.planning/phases/01-foundation-shared-chrome/01-03-SUMMARY.md

<interfaces>
<!-- Components available from Phase 1 -->

From src/components/FetchLogo.tsx:
```typescript
export type FetchLogoProps = {
  size?: number;   // default 100
  color?: string;  // default '#2463EC'
  title?: string;  // default 'Fetch'
};
export function FetchLogo(props: FetchLogoProps): JSX.Element;
export default FetchLogo;
```

<!-- Note: `/` splash is NOT inside FlowLayout per spec. The spec says
     "centered on #EBF5FF" with NO white panel — splash sits directly on
     the page background. FlowLayout is for the panel screens
     (/welcome, /permissions, etc.). -->

<!-- Theme tokens available (src/theme/theme.ts) -->
- theme.palette.background.default = '#EBF5FF' (use as `bgcolor: 'background.default'`)
- theme.palette.text.primary       = '#101827' (use as `color: 'text.primary'`)
- theme.palette.primary.main       = '#2463EC' (used by FetchLogo internally)
- typography.fontFamily            = 'var(--font-inter), system-ui, sans-serif'

<!-- Next.js 15 App Router client-side navigation -->
```typescript
import { useRouter } from 'next/navigation';
// inside a 'use client' component:
const router = useRouter();
router.push('/welcome'); // client-side route change, no full reload
```

<!-- Emotion keyframes (already in the project's MUI peer chain — @emotion/react ^11.14.0 in package.json) -->
```typescript
import { keyframes } from '@emotion/react';
const myAnim = keyframes`
  from { transform: scale(0.8); opacity: 0; }
  to   { transform: scale(1);   opacity: 1; }
`;
// usage: <Box sx={{ animation: `${myAnim} 500ms ease-out` }} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite `/` as the real splash with logo, tagline, animations, and auto-redirect to /welcome</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (current Phase 1 stub — being fully replaced)
    - Main_Fetch_Gateway.md (spec section `### \`/\` — Splash` — line ~68: tagline "Retirement runs on Fetch", scale-in 500ms, breathing 2s cycle, auto-redirect to /welcome after 2500ms)
    - .planning/ROADMAP.md (Phase 2 Success Criterion 1 — defines the externally observable behavior)
    - src/components/FetchLogo.tsx (the component being mounted — confirms it accepts `size` prop, renders inline SVG)
    - src/theme/theme.ts (theme tokens — `background.default` for page bg, `text.primary` for tagline)
    - package.json (confirms `@emotion/react` ^11.14.0 is available for `keyframes` import — no new dep)
  </read_first>
  <action>
    Fully overwrite `src/app/page.tsx`. The new file is a Client Component (top-of-file `'use client'` directive — required because the splash uses `useEffect` for the redirect timer and CSS keyframes). Build it as follows:

    1. Imports (exact set):
       - `'use client'` (the literal directive on line 1).
       - `useEffect` from `'react'`.
       - `useRouter` from `'next/navigation'`.
       - `keyframes` from `'@emotion/react'`.
       - `Box`, `Stack`, `Typography` from `'@mui/material'` (or per-module imports — match the existing repo style of per-module deep imports as seen in Phase 1 stubs).
       - `FetchLogo` from `'@/components/FetchLogo'`.

    2. Define two `keyframes` constants at module scope (not inside the component, to avoid recomputation):
       - `scaleIn`: `from { transform: scale(0.6); opacity: 0; } to { transform: scale(1); opacity: 1; }`
       - `breathe`: `0% { transform: scale(1); } 50% { transform: scale(1.04); } 100% { transform: scale(1); }` — subtle 4% pulse, not jarring.

    3. The default-exported component renders:
       - Outer `<Box>` with `sx`: `minHeight: '100vh'`, `bgcolor: 'background.default'` (= `#EBF5FF` via theme — NEVER hardcode the hex), `display: 'flex'`, `alignItems: 'center'`, `justifyContent: 'center'`.
       - Inside, a centered `<Stack>` with `spacing={3}` and `sx={{ alignItems: 'center' }}` (use the canonical `alignItems` in sx workaround per the Plan 01-02 SUMMARY decision — MUI v9.0.1 Stack typing regression).
       - Inside the Stack, a wrapping `<Box>` whose `sx.animation` chains the two keyframes: `animation: `${scaleIn} 500ms ease-out, ${breathe} 2s ease-in-out 500ms infinite`` — the breathe animation has a 500ms delay so it starts exactly when scale-in finishes, then loops every 2s.
       - Inside that animated Box, render `<FetchLogo size={100} />` (use the default brand color).
       - Below the Stack-internal animated logo wrapper, render `<Typography variant="h6">` (or `body1` if h6 feels heavy — pick `h6`) with `sx={{ color: 'text.primary', fontWeight: 500 }}` and the EXACT text `Retirement runs on Fetch`. (No period. Match the spec literal.)

    4. Inside the component body, before the return:
       - Call `const router = useRouter();`.
       - Use `useEffect(() => { ... }, [router]);`:
         - `const timer = setTimeout(() => { router.push('/welcome'); }, 2500);`
         - Return cleanup: `return () => clearTimeout(timer);`
       - The dependency array is `[router]` (matches React lint guidance; the router is a stable ref but lint will demand it).

    5. Do NOT use `FlowLayout` here — `/` splash is the only screen that sits on the bare page background with no white panel (per the spec section `### / — Splash`). Verify the Phase 1 stub used `FlowLayout` only as a smoke convenience; this real screen drops it.

    Do NOT add `console.log`. Do NOT use `any`. Do NOT hardcode `#EBF5FF` (use `bgcolor: 'background.default'`). Do NOT add a `setInterval`. Do NOT add real OAuth, fetches, or persistence. Do NOT prefetch `/welcome` explicitly (`router.push` already does instant client-side routing).

    Exact timings to encode (do not deviate):
    - Scale-in: 500ms.
    - Breathe: 2000ms (2s) cycle, infinite, starting at 500ms (so it kicks in after scale-in).
    - Auto-redirect: 2500ms via `setTimeout`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "'use client'" src/app/page.tsx && grep -c "router.push" src/app/page.tsx && grep -c "Retirement runs on Fetch" src/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File starts with `'use client'` directive on the first non-blank line.
      - check: `head -3 src/app/page.tsx | grep -c "'use client'"` returns 1
    - The exact tagline string appears verbatim.
      - check: `grep -cF "Retirement runs on Fetch" src/app/page.tsx` returns 1
    - The auto-redirect target is `/welcome` via `router.push`.
      - check: `grep -cE "router\\.push\\(['\"]\\/welcome['\"]\\)" src/app/page.tsx` returns 1
    - The redirect timeout is exactly 2500ms.
      - check: `grep -cE "setTimeout\\([^,]+,\\s*2500\\)" src/app/page.tsx` returns 1
    - The cleanup function returns clearTimeout (no leaked timer).
      - check: `grep -cE "clearTimeout" src/app/page.tsx` returns at least 1
    - The scale-in animation runs 500ms.
      - check: `grep -cE "500ms" src/app/page.tsx` returns at least 1
    - The breathing animation is 2s and loops infinitely.
      - check: `grep -cE "2s[^\"']*infinite|infinite[^\"']*2s" src/app/page.tsx` returns at least 1
    - The page uses the theme background token, not the hex literal.
      - check: `grep -cE "bgcolor:\\s*['\"]background\\.default['\"]" src/app/page.tsx` returns 1
      - check: `grep -cF "#EBF5FF" src/app/page.tsx` returns 0
    - `FetchLogo` is rendered (component name appears) and `FlowLayout` is NOT used (splash sits on bare page bg per spec).
      - check: `grep -cF "<FetchLogo" src/app/page.tsx` returns at least 1
      - check: `grep -cF "FlowLayout" src/app/page.tsx` returns 0
    - The Phase 1 placeholder strings are gone.
      - check: `grep -cF "Splash (\`/\`)" src/app/page.tsx` returns 0
      - check: `grep -cF "Phase 1 placeholder" src/app/page.tsx` returns 0
    - TypeScript compiles with no errors.
      - check: `npx tsc --noEmit` exits 0
    - Hygiene gates from CLAUDE.md hold for this file.
      - check: `grep -cE "console\\.log" src/app/page.tsx` returns 0
      - check: `grep -cE "(^|[^a-zA-Z_]): *any( |;|,|>|$)" src/app/page.tsx` returns 0
    - Live HTTP smoke: with `npm run dev` running on 3001, `curl -s http://localhost:3001/` returns 200 and the body contains both the literal `Retirement runs on Fetch` and the FetchLogo `<title>Fetch</title>` SVG marker.
      - check: `curl -s -o /tmp/page.html -w "%{http_code}" http://localhost:3001/` returns 200
      - check: `grep -cF "Retirement runs on Fetch" /tmp/page.html` returns at least 1
      - check: `grep -cF "<title>Fetch</title>" /tmp/page.html` returns at least 1
  </acceptance_criteria>
  <done>
    `/` renders the Fetch logo on `#EBF5FF` with the "Retirement runs on Fetch" tagline; the logo scales in over 500ms and then breathes on a 2s cycle; after 2500ms `useRouter().push('/welcome')` fires; the timer is cleaned up on unmount. `tsc --noEmit` exits 0 and the route returns 200 under `npm run dev`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side route navigation with no user input parsing, no network calls, no persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | DoS-self | setTimeout in useEffect | mitigate | useEffect cleanup returns `clearTimeout(timer)` so navigating away or unmounting cancels the redirect. Acceptance criteria asserts the presence of `clearTimeout`. |
| T-02-02-02 | Tampering | Auto-redirect target | accept | Target `/welcome` is a static literal — no untrusted input flows into `router.push`. The auto-redirect is part of the demo trust narrative, not a security mechanism. |
</threat_model>

<verification>
- `npx tsc --noEmit` → exit 0.
- The eight grep gates in `<acceptance_criteria>` all pass.
- Live smoke under `npm run dev`: `/` returns 200 with tagline + FetchLogo SVG in the SSR markup; client-side `setTimeout` fires `router.push('/welcome')` after 2.5s (verified by visiting in browser; SSR pre-renders the splash markup).
</verification>

<success_criteria>
- FLOW-01 satisfied: `/` shows the Fetch logo on `#EBF5FF` with the tagline, 500ms scale-in, 2s breathing pulse, and auto-redirect to `/welcome` after ~2500ms.
- No `console.log`, no `any`, no `#EBF5FF` hex literal in the file (theme token used instead).
- ROADMAP Phase 2 Success Criterion 1 is observable end-to-end in the browser.
</success_criteria>

<output>
Create `.planning/phases/02-pre-provider-flow/02-02-SUMMARY.md` when done. Include:
- One-line summary of the splash screen + animations + auto-redirect.
- Confirmation of FLOW-01 requirement satisfaction.
- Verification evidence: `tsc --noEmit` exit code, the eight greps from `<acceptance_criteria>`, and the live HTTP smoke result.
- Note that the Phase 1 placeholder strings (`Splash (\`/\`)`, `Phase 1 placeholder`) are gone from `src/app/page.tsx`.
</output>
