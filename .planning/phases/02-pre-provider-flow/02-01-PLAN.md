---
phase: 02-pre-provider-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/FlowLayout.tsx
autonomous: true
requirements:
  - WR-01
  - WR-02
tags: [mui, flow-layout, api-fix, phase-1-review-followup]

must_haves:
  truths:
    - "Plan 02-03 can land /welcome with the spec's 48px uniform panel padding through FlowLayout's API (no Box wrapper hack)"
    - "Plan 02-04 can land /permissions with the spec's 48px vertical / 36px horizontal panel padding through FlowLayout's API (no Box wrapper hack)"
    - "Every existing Phase 1 route stub (`/`, `/welcome`, `/permissions`, `/select-provider`, `/connecting`, `/success`) still renders with 48px uniform padding when no padding props are passed — no visual regression"
    - "tsc --noEmit exits 0 and ESLint reports zero new errors after the API change"
  artifacts:
    - path: "src/components/FlowLayout.tsx"
      provides: "FlowLayout component with `px` + `py` props (theme-spacing units) replacing the old single `padding: number` prop"
      contains: "px?: number"
      contains_also: "py?: number"
  key_links:
    - from: "src/app/welcome/page.tsx (Plan 02-03)"
      to: "src/components/FlowLayout.tsx"
      via: "<FlowLayout px={6} py={6}> = 48px/48px uniform padding"
      pattern: "px=\\{6\\}.*py=\\{6\\}|py=\\{6\\}.*px=\\{6\\}"
    - from: "src/app/permissions/page.tsx (Plan 02-04)"
      to: "src/components/FlowLayout.tsx"
      via: "<FlowLayout px={4.5} py={6}> = 36px/48px split padding"
      pattern: "px=\\{4\\.5\\}|py=\\{6\\}"
---

<objective>
Widen `FlowLayout`'s padding API from a single `padding: number` prop (which the Phase 1 review WR-01/WR-02 flagged as semantically broken and shape-wrong) to a `px?` / `py?` pair that consumes MUI theme-spacing units. This unblocks Plan 02-03's 48px uniform `/welcome` panel and Plan 02-04's 48px-vertical / 36px-horizontal `/permissions` panel, both demanded verbatim by `Main_Fetch_Gateway.md`.

Purpose:
- Closes Phase 1 REVIEW findings WR-01 (px-string interpolation defeats theme spacing) and WR-02 (single scalar can't express the spec's split horizontal/vertical permissions padding).
- Lets the three Wave-2 screen plans run fully in parallel — each owns exactly one route file, none touches FlowLayout.
- Keeps default behavior identical for the three existing route stubs untouched in this phase (`/select-provider`, `/connecting`, `/success`).

Output:
- `src/components/FlowLayout.tsx` updated with new prop shape.
- Default behavior: `px=6 py=6` → 48px uniform padding (matches the spec default and current Phase 1 visual).
- `padding` prop removed (it was dead — no caller passed it).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./Main_Fetch_Gateway.md
@.planning/phases/01-foundation-shared-chrome/01-02-SUMMARY.md
@.planning/phases/01-foundation-shared-chrome/01-REVIEW.md

<interfaces>
<!-- Current FlowLayout API (from src/components/FlowLayout.tsx after Phase 1) -->

```typescript
export type FlowLayoutProps = {
  children: ReactNode;
  maxWidth?: number;
  padding?: number; // BROKEN: interpolated as `${padding}px`, bypasses theme.spacing
};

export function FlowLayout({
  children,
  maxWidth = 440,
  padding = 48,
}: FlowLayoutProps): JSX.Element;
```

<!-- Six current callers, all using only the default (none pass `padding`) -->

- src/app/page.tsx                   → <FlowLayout maxWidth={440}>
- src/app/welcome/page.tsx           → <FlowLayout maxWidth={440}>
- src/app/permissions/page.tsx       → <FlowLayout maxWidth={768}>
- src/app/select-provider/page.tsx   → <FlowLayout maxWidth={498}>
- src/app/connecting/page.tsx        → <FlowLayout maxWidth={440}>
- src/app/success/page.tsx           → <FlowLayout maxWidth={440}>

<!-- Target FlowLayout API (after this plan) -->

```typescript
export type FlowLayoutProps = {
  children: ReactNode;
  maxWidth?: number;
  px?: number;  // theme-spacing units; defaults to 6 (= 48px)
  py?: number;  // theme-spacing units; defaults to 6 (= 48px)
};
```

<!-- MUI theme spacing in this repo: theme.spacing(N) === N * 8px (MUI default).
     So px={6} = 48px, px={4.5} = 36px, py={6} = 48px. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen FlowLayout's padding API to px + py (theme-spacing units)</name>
  <files>src/components/FlowLayout.tsx</files>
  <read_first>
    - src/components/FlowLayout.tsx (current implementation — must not visually regress)
    - .planning/phases/01-foundation-shared-chrome/01-REVIEW.md (WR-01 and WR-02 are the rationale; the fix should match WR-02's "Option" preemptive widening)
    - Main_Fetch_Gateway.md (spec lines for /welcome "padding 48px" and /permissions "padding 48px vertical 36px" — these are the two consumer shapes the new API must serve)
    - .planning/phases/01-foundation-shared-chrome/01-02-SUMMARY.md (existing FlowLayout decisions — keep theme tokens, keep the brand-spec literals for borderRadius and boxShadow)
  </read_first>
  <action>
    Edit `src/components/FlowLayout.tsx` to replace the `padding?: number` prop with two new props: `px?: number` and `py?: number`. Both accept MUI theme-spacing units (the MUI default of 8px/unit applies — `px={6}` resolves to 48px, `px={4.5}` resolves to 36px).

    Concrete edits inside the file:

    1. Update the `FlowLayoutProps` type:
       - Remove the `padding?: number` property.
       - Add `px?: number` and `py?: number`, both optional, both `number`.
       - Update the JSDoc above the props (or comment block at top of file) to state: defaults are `px={6}` and `py={6}` = 48px uniform, matching the spec's /welcome panel; /permissions passes `px={4.5} py={6}` for 36px/48px split.

    2. Update the component destructure:
       - Remove `padding = 48`.
       - Add `px = 6` and `py = 6` (both default to 6 theme-spacing units = 48px).

    3. Update the Paper's `sx`:
       - Remove the line `p: `${padding}px``.
       - Replace it with `px,` and `py,` (theme-spacing aware, no string interpolation).

    4. Keep everything else identical:
       - The outer Box `sx` block (minHeight: '100vh', bgcolor: 'background.default', flex centering, px: 2) stays.
       - The Paper's `borderRadius: '12px'`, `boxShadow: '0 2px 8px rgba(0,0,0,0.08)'`, `width: '100%'`, and `maxWidth` stay.
       - The default-export and named-export shape stays — no breaking change to consumers that don't pass padding.

    Do NOT touch the six route stub files in this task — they all rely on the default, which still resolves to 48px uniform after this change. No `'use client'` directive. No new imports. The brand-spec literals (12px radius, the shadow) remain as the only hardcoded values per the canonical decision in Plan 01-02's SUMMARY.

    Address WR-01 (per Phase 1 REVIEW) by switching from raw-px string interpolation to native theme-spacing numbers. Address WR-02 (per Phase 1 REVIEW) by splitting padding into px/py so the /permissions screen can express its 36px-horizontal / 48px-vertical requirement without an `sx` override at the call site.

    Do not change FetchLogo, theme.ts, or any route stub in this task. Do not add any client-side hooks. Do not delete the JSDoc block at the top of the file — update it in place to describe the new prop shape.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/FlowLayout.tsx` exports a type `FlowLayoutProps` whose properties match exactly: `children`, `maxWidth`, `px`, `py` (the literal string `padding?:` MUST NOT appear in the file).
      - check: `grep -nE "px\\?:[[:space:]]*number" src/components/FlowLayout.tsx` returns exactly one match
      - check: `grep -nE "py\\?:[[:space:]]*number" src/components/FlowLayout.tsx` returns exactly one match
      - check: `grep -cE "padding\\?:[[:space:]]*number" src/components/FlowLayout.tsx` returns 0
    - The component default values resolve `px` to 6 and `py` to 6.
      - check: `grep -nE "px[[:space:]]*=[[:space:]]*6" src/components/FlowLayout.tsx` returns at least one match
      - check: `grep -nE "py[[:space:]]*=[[:space:]]*6" src/components/FlowLayout.tsx` returns at least one match
    - Paper `sx` no longer interpolates `${padding}px`.
      - check: `grep -cE "\\\$\\{padding\\}px" src/components/FlowLayout.tsx` returns 0
    - Brand-spec values are preserved verbatim.
      - check: `grep -cE "borderRadius:[[:space:]]*'12px'" src/components/FlowLayout.tsx` returns 1
      - check: `grep -cE "boxShadow:[[:space:]]*'0 2px 8px rgba\\(0,0,0,0\\.08\\)'" src/components/FlowLayout.tsx` returns 1
    - TypeScript compiles repo-wide with no errors after the change.
      - check: `npx tsc --noEmit` exits 0
    - The six route stub pages still compile unchanged (no caller passes the removed `padding` prop, per the Phase 1 SUMMARYs).
      - check: `grep -rnE "<FlowLayout[^>]*padding=" src/app` returns no matches (no caller was passing `padding`, so removing it cannot break any caller)
    - No `console.log` or `: any` introduced.
      - check: `grep -nE "console\\.log" src/components/FlowLayout.tsx` returns no matches
      - check: `grep -nE "(^|[^a-zA-Z_]): *any( |;|,|>|$)" src/components/FlowLayout.tsx` returns no matches
  </acceptance_criteria>
  <done>
    FlowLayout exports `px?: number` and `py?: number` with defaults 6/6, applies them through MUI's native theme-spacing on the Paper, `tsc --noEmit` exits 0, and every existing route still renders at the same visual size (48px uniform panel padding).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Plan 02-01 is a pure refactor of a presentational component. No network, no IO, no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | FlowLayout API shape | accept | Refactor preserves the existing default-export signature; consumers that don't pass padding props see no behavior change. Visual smoke against existing stubs in Plan 02-02/02-03/02-04 acceptance criteria catches regression. |
</threat_model>

<verification>
- `npx tsc --noEmit` → exit 0
- `grep -cE "padding\\?:" src/components/FlowLayout.tsx` → 0 (old prop gone)
- `grep -cE "(px|py)\\?:[[:space:]]*number" src/components/FlowLayout.tsx` → 2 (new props in)
- Visual smoke (in Wave 2 plans): each route still renders the centered white panel on `#EBF5FF` at the spec-mandated size.
</verification>

<success_criteria>
- FlowLayout's padding API is `px?: number` + `py?: number` (theme-spacing units).
- Default props yield 48px uniform panel padding (no visual change for existing route stubs).
- Phase 1 REVIEW findings WR-01 and WR-02 are closed.
- Wave 2 plans (02-02, 02-03, 02-04) can run fully in parallel — none needs to touch FlowLayout.
</success_criteria>

<output>
Create `.planning/phases/02-pre-provider-flow/02-01-SUMMARY.md` when done. Include:
- One-line summary of the FlowLayout API change.
- Confirmation that no callers required changes (the `padding` prop was dead per Phase 1 REVIEW WR-02).
- Note that WR-01 + WR-02 from `01-REVIEW.md` are resolved.
- Verification evidence (the four greps above + `tsc --noEmit` exit code).
</output>
