---
phase: 02-pre-provider-flow
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/app/welcome/page.tsx
autonomous: true
requirements:
  - FLOW-02
tags: [mvp, vertical-slice, welcome, mui-button, client-component, flow-layout-consumer]

must_haves:
  truths:
    - "Visiting `/welcome` shows a centered white panel (440px wide) on the `#EBF5FF` page background — no Phase 1 placeholder heading, no muted hint"
    - "The white panel has 48px padding on all four sides (via FlowLayout's new px/py API from Plan 02-01)"
    - "Inside the panel, the user sees the Fetch logo at the top, the heading 'Connect your payroll provider', the explanatory body copy, and a primary 'Get Started' MUI Button below"
    - "Clicking 'Get Started' navigates the user to `/permissions` via Next.js client-side routing (no full page reload)"
  artifacts:
    - path: "src/app/welcome/page.tsx"
      provides: "/welcome real screen — FetchLogo + heading + body copy + Get Started button → /permissions"
      contains: "'use client'"
      contains_also: "Connect your payroll provider"
      contains_also: "Get Started"
      contains_also: "/permissions"
  key_links:
    - from: "src/app/welcome/page.tsx"
      to: "next/navigation router"
      via: "<Button onClick={() => router.push('/permissions')}>"
      pattern: "router\\.push\\(['\"]\\/permissions['\"]\\)"
    - from: "src/app/welcome/page.tsx"
      to: "src/components/FlowLayout.tsx"
      via: "<FlowLayout maxWidth={440} px={6} py={6}>"
      pattern: "maxWidth=\\{440\\}.*px=\\{6\\}|px=\\{6\\}.*py=\\{6\\}"
---

<objective>
Replace the Phase 1 `/welcome` placeholder with the real welcome screen: a centered 440px-wide white panel on `#EBF5FF` with 48px uniform padding, the Fetch logo (100px), the heading "Connect your payroll provider", the spec's explanatory body paragraph, and a primary "Get Started" MUI Button that navigates to `/permissions`.

Purpose:
- Delivers FLOW-02 in a single vertical slice — the user enters from the splash auto-redirect, reads what they're being asked to do, and clicks one button to advance.
- First real consumer of Plan 02-01's new FlowLayout `px`/`py` API (uses `px={6}` `py={6}` for 48px uniform per spec).
- Establishes the canonical button + router pattern (`'use client'` + `useRouter().push`) that `/permissions` (Plan 02-04) and the rest of the flow will mirror.

Output:
- `src/app/welcome/page.tsx` rewritten as a Client Component rendering the real welcome screen.
- No new components, no new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./Main_Fetch_Gateway.md
@.planning/phases/01-foundation-shared-chrome/01-02-SUMMARY.md
@.planning/phases/01-foundation-shared-chrome/01-03-SUMMARY.md
@.planning/phases/02-pre-provider-flow/02-01-PLAN.md

<interfaces>
<!-- Components available (Phase 1 + Plan 02-01) -->

From src/components/FlowLayout.tsx (after Plan 02-01 lands):
```typescript
export type FlowLayoutProps = {
  children: ReactNode;
  maxWidth?: number;  // default 440
  px?: number;        // theme-spacing units; default 6 (= 48px)
  py?: number;        // theme-spacing units; default 6 (= 48px)
};
export function FlowLayout(props: FlowLayoutProps): JSX.Element;
export default FlowLayout;
```

From src/components/FetchLogo.tsx:
```typescript
export type FetchLogoProps = { size?: number; color?: string; title?: string };
export function FetchLogo(props: FetchLogoProps): JSX.Element;
export default FetchLogo;
```

<!-- MUI Button: import from '@mui/material/Button' (per-module path matches repo style) -->
<!-- Use variant="contained" for the primary brand-blue fill, color="primary" reads theme primary -->

<!-- Next.js 15 App Router client routing -->
```typescript
import { useRouter } from 'next/navigation';
const router = useRouter();
router.push('/permissions');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite /welcome as the real welcome screen with heading, body copy, and primary Get Started button → /permissions</name>
  <files>src/app/welcome/page.tsx</files>
  <read_first>
    - src/app/welcome/page.tsx (current Phase 1 stub being fully replaced)
    - Main_Fetch_Gateway.md (spec section `### /welcome — Welcome` lines ~71-77 — the exact heading and body copy are sourced from here)
    - .planning/ROADMAP.md (Phase 2 Success Criterion 2 — defines externally observable behavior)
    - .planning/phases/02-pre-provider-flow/02-01-PLAN.md (FlowLayout's new px/py API — this is the FIRST consumer)
    - src/components/FlowLayout.tsx (post Plan 02-01 — confirms props are maxWidth/px/py)
    - src/components/FetchLogo.tsx (confirms size prop default is 100)
    - src/theme/theme.ts (theme.palette.primary.main = #2463EC; theme.palette.text.primary, text.secondary)
  </read_first>
  <action>
    Fully overwrite `src/app/welcome/page.tsx`. Build the file as follows:

    1. First non-blank line: `'use client'` directive (required because the Button uses `onClick` → `router.push`).

    2. Imports (exact set):
       - `useRouter` from `'next/navigation'`.
       - `Stack` from `'@mui/material/Stack'`.
       - `Typography` from `'@mui/material/Typography'`.
       - `Button` from `'@mui/material/Button'`.
       - `FlowLayout` from `'@/components/FlowLayout'`.
       - `FetchLogo` from `'@/components/FetchLogo'`.

    3. The default-exported `Page` component:
       - Calls `const router = useRouter();` at the top of its body.
       - Returns `<FlowLayout maxWidth={440} px={6} py={6}>` (440px panel, 48px uniform padding per spec).
       - Inside FlowLayout, a single `<Stack spacing={3} sx={{ alignItems: 'center' }}>` (alignItems-in-sx per the codebase-wide MUI v9.0.1 workaround documented in Plan 01-02 SUMMARY).
       - Inside the Stack, in this exact order:
         a. `<FetchLogo size={100} />`
         b. `<Typography variant="h5" component="h1" sx={{ fontWeight: 700, color: 'text.primary', textAlign: 'center' }}>Connect your payroll provider</Typography>` — the EXACT spec heading verbatim, no period.
         c. `<Typography variant="body1" sx={{ color: 'text.secondary', textAlign: 'center' }}>Plantegrity has requested a secure read-only connection to your payroll data for plan validation and reconciliation. No data will be modified.</Typography>` — EXACT spec body copy verbatim (from Main_Fetch_Gateway.md line ~75). Punctuation, capitalization, and the trailing period must match the spec literal.
         d. `<Button variant="contained" color="primary" size="large" onClick={() => router.push('/permissions')} sx={{ mt: 2, alignSelf: 'stretch', textTransform: 'none', fontWeight: 600 }}>Get Started</Button>` — primary brand-blue, "Get Started" label exactly (capital G capital S), `textTransform: 'none'` because MUI defaults to uppercased button text and the spec uses sentence case.

    4. Constraints to honor while writing:
       - No hardcoded hex codes. Brand colors come from theme tokens (`color: 'text.primary'`, `color: 'text.secondary'`, `color="primary"`). The `#2463EC` Fetch blue is supplied by `color="primary"` on the Button (theme palette) and by FetchLogo's internal default.
       - No `console.log`. No `: any`. No new dependency adds.
       - No `Link` from `next/link` — use the `onClick` handler with `router.push` (matches the splash auto-redirect pattern; consistent imperative navigation style across the flow).
       - The Phase 1 placeholder text (`Welcome (\`/welcome\`)`, `Phase 1 placeholder — real welcome screen lands in Phase 2 (FLOW-02).`) must be completely gone.

    Concrete spec values being encoded:
    - Panel max-width: 440px (`maxWidth={440}`).
    - Panel padding: 48px uniform (`px={6} py={6}` against theme.spacing=8px default → 48px).
    - FetchLogo size: 100px.
    - Heading: "Connect your payroll provider" (no period).
    - Body: "Plantegrity has requested a secure read-only connection to your payroll data for plan validation and reconciliation. No data will be modified."
    - Button label: "Get Started".
    - Button route: `/permissions`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "Connect your payroll provider" src/app/welcome/page.tsx && grep -c "Get Started" src/app/welcome/page.tsx && grep -cE "router\.push\(['\"]\/permissions['\"]\)" src/app/welcome/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File starts with `'use client'` directive.
      - check: `head -3 src/app/welcome/page.tsx | grep -c "'use client'"` returns 1
    - The exact heading string appears verbatim.
      - check: `grep -cF "Connect your payroll provider" src/app/welcome/page.tsx` returns 1
    - The exact body copy appears verbatim.
      - check: `grep -cF "Plantegrity has requested a secure read-only connection to your payroll data for plan validation and reconciliation. No data will be modified." src/app/welcome/page.tsx` returns 1
    - The Button label is exactly "Get Started".
      - check: `grep -cF ">Get Started<" src/app/welcome/page.tsx` returns 1
    - The button navigates to /permissions via router.push.
      - check: `grep -cE "router\\.push\\(['\"]\\/permissions['\"]\\)" src/app/welcome/page.tsx` returns 1
    - The panel uses FlowLayout with the spec-mandated 440px width and 48px padding (via new px/py API).
      - check: `grep -cE "<FlowLayout[^>]*maxWidth=\\{440\\}" src/app/welcome/page.tsx` returns 1
      - check: `grep -cE "px=\\{6\\}" src/app/welcome/page.tsx` returns 1
      - check: `grep -cE "py=\\{6\\}" src/app/welcome/page.tsx` returns 1
    - The FetchLogo is rendered at the 100px size.
      - check: `grep -cE "<FetchLogo[^>]*size=\\{100\\}" src/app/welcome/page.tsx` returns 1
    - The Button is the primary MUI variant.
      - check: `grep -cE "variant=['\"]contained['\"]" src/app/welcome/page.tsx` returns at least 1
      - check: `grep -cE "color=['\"]primary['\"]" src/app/welcome/page.tsx` returns at least 1
    - No raw hex brand colors leaked into the file.
      - check: `grep -cF "#2463EC" src/app/welcome/page.tsx` returns 0
      - check: `grep -cF "#EBF5FF" src/app/welcome/page.tsx` returns 0
    - The Phase 1 placeholder strings are gone.
      - check: `grep -cF "Welcome (\`/welcome\`)" src/app/welcome/page.tsx` returns 0
      - check: `grep -cF "Phase 1 placeholder" src/app/welcome/page.tsx` returns 0
    - TypeScript compiles with no errors.
      - check: `npx tsc --noEmit` exits 0
    - Hygiene gates from CLAUDE.md hold for this file.
      - check: `grep -cE "console\\.log" src/app/welcome/page.tsx` returns 0
      - check: `grep -cE "(^|[^a-zA-Z_]): *any( |;|,|>|$)" src/app/welcome/page.tsx` returns 0
    - Live HTTP smoke: with `npm run dev` running on 3001, `curl -s http://localhost:3001/welcome` returns 200 and the body contains the heading, the body copy, and the button label.
      - check: `curl -s -o /tmp/welcome.html -w "%{http_code}" http://localhost:3001/welcome` returns 200
      - check: `grep -cF "Connect your payroll provider" /tmp/welcome.html` returns at least 1
      - check: `grep -cF "Plantegrity has requested a secure read-only connection" /tmp/welcome.html` returns at least 1
      - check: `grep -cF "Get Started" /tmp/welcome.html` returns at least 1
  </acceptance_criteria>
  <done>
    `/welcome` renders the 440px white panel on `#EBF5FF` with 48px padding, FetchLogo + heading + body copy + primary "Get Started" button. Clicking the button calls `router.push('/permissions')`. `tsc --noEmit` exits 0 and `curl /welcome` returns 200 with the spec copy in the SSR markup.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Static content with one client-side button navigation; no user input, no network calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | router.push target | accept | Target `/permissions` is a static literal — no untrusted input. The button is a single-purpose navigation control. |
</threat_model>

<verification>
- `npx tsc --noEmit` → exit 0.
- The 14 grep gates in `<acceptance_criteria>` all pass.
- Live `curl http://localhost:3001/welcome` returns 200 with the heading + body copy + button label in the SSR markup.
- ROADMAP Phase 2 Success Criterion 2 observable in a browser: panel is 440px wide, 48px padded, heading is "Connect your payroll provider", button navigates to `/permissions`.
</verification>

<success_criteria>
- FLOW-02 satisfied: centered 440px white panel, 48px padding, FetchLogo + heading + body + primary "Get Started" button → `/permissions`.
- No dead button — `Get Started` lands on a real route.
- No hex literals leaked; all colors come from theme tokens.
- ROADMAP Phase 2 Success Criterion 2 observable end-to-end.
</success_criteria>

<output>
Create `.planning/phases/02-pre-provider-flow/02-03-SUMMARY.md` when done. Include:
- One-line summary of the welcome screen rebuild.
- Confirmation of FLOW-02 requirement satisfaction and that this is the first consumer of FlowLayout's new px/py API.
- Verification evidence: `tsc --noEmit` exit code, the 14 greps from `<acceptance_criteria>`, and the live HTTP smoke result.
- Confirmation that the Phase 1 placeholder strings are gone from `src/app/welcome/page.tsx`.
</output>
