---
phase: 06-demo-home
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/app/gusto-login/page.tsx
  - src/app/connect-method/page.tsx
  - package.json
autonomous: true
requirements: [DEMO-01, DEMO-02, DEMO-03]

must_haves:
  truths:
    - "From `/` the user sees a Fetch-branded launcher with three OptionRows and can start each of the three demo flows."
    - "`/` no longer auto-redirects to `/welcome`; it renders the launcher and stays."
    - "Choosing Gusto on `/connect-method` opens the bespoke `/gusto-login` mock (Gusto-branded, not Fetch DS)."
    - "On `/gusto-login` the user can sign in (step 1 → step 2) and Authorize → returns to the Fetch flow at `/connecting?provider=gusto` → `/success`; Deny → `/connect-method?provider=gusto`."
    - "Choosing SFTP opens a DS-styled modal with Host / Username or Email / Password fields and the encrypted-never-stored caption, then Sign In → `/connecting?provider=sftp` → `/success`."
    - "Choosing Principal opens the creds modal → `/connecting?provider=principal&2fa=1` → `/verify` → `/success` (unchanged)."
    - "`npm run lint:tokens` passes with the Gusto bespoke hex living only in `src/app/gusto-login/` (excluded), and zero off-token hex/px elsewhere."
    - "tsc, lint, lint:tokens, and build all pass; no dead buttons; no new dependencies."
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Demo home launcher (DEMO-01)"
      contains: "Fetch demo"
    - path: "src/app/gusto-login/page.tsx"
      provides: "Bespoke 2-step Gusto mock (DEMO-02)"
      contains: "Authorize Fetch to connect"
    - path: "src/app/connect-method/page.tsx"
      provides: "Gusto→/gusto-login rewire + SFTP modal per Figma 8:365 (DEMO-03)"
      contains: "Sign in to SFTP"
    - path: "package.json"
      provides: "lint:tokens excludes src/app/gusto-login"
      contains: "src/app/gusto-login"
  key_links:
    - from: "src/app/page.tsx"
      to: "/sign-up?org=existing | /sign-up | /welcome"
      via: "OptionRow onClick → router.push"
      pattern: "router\\.push\\('/sign-up\\?org=existing'\\)"
    - from: "src/app/connect-method/page.tsx"
      to: "/gusto-login"
      via: "handleSelfClick redirect branch"
      pattern: "router\\.push\\('/gusto-login'\\)"
    - from: "src/app/gusto-login/page.tsx"
      to: "/connecting?provider=gusto"
      via: "Authorize button → router.push"
      pattern: "router\\.push\\('/connecting\\?provider=gusto'\\)"
---

<objective>
Build the Phase 06 "Demo Home" slice: repurpose `/` into a launcher for the three demo flows, add a bespoke 2-step Gusto OAuth mock at `/gusto-login`, and make the three connection types tangible on `/connect-method` (Gusto redirect → the mock; SFTP modal refined to Figma 8:365; Principal creds→2FA confirmed unchanged).

Purpose: Turn the navigable-but-abstract demo into a self-serve launcher with realistic per-provider credential moments — the polished demo wedge HR/payroll admins will see.
Output: Rewritten `src/app/page.tsx`, new `src/app/gusto-login/page.tsx`, edited `src/app/connect-method/page.tsx`, and an updated `lint:tokens` script in `package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-demo-home/DEMO-HOME-SPEC.md
@.planning/REQUIREMENTS.md
@./CLAUDE.md

@src/app/page.tsx
@src/app/connect-method/page.tsx
@src/lib/providers.ts
@package.json

<interfaces>
<!-- DS component + catalog contracts the executor needs. Extracted from the codebase — use directly, do NOT re-explore. -->

From src/lib/providers.ts:
```typescript
export type Provider = {
  slug: 'gusto' | 'principal' | 'sftp';
  name: string;
  authMethod: 'redirect' | 'credentials' | 'sftp';
};
// gusto→redirect, principal→credentials, sftp→sftp
```

From src/components/OptionRow.tsx (default export `OptionRow`):
```typescript
export type OptionRowProps = {
  title: string;
  description?: string;
  selected?: boolean;
  disabled?: boolean;
  onClick?: () => void;
};
```

From src/components/Input.tsx (default export `Input`):
```typescript
export type InputProps = {
  label?: string;
  value: string;
  onChange: (e: React.ChangeEvent<HTMLInputElement>) => void;
  placeholder?: string;
  type?: string;        // 'password' for the password field
  error?: boolean;
  helperText?: string;
  disabled?: boolean;
  fullWidth?: boolean;  // defaults true
};
```

From src/components/Button.tsx (default export `Button`):
```typescript
// variant 'primary'|'secondary' (default primary), size 'sm'|'md'|'lg', loading, iconStart.
// Extends MUI ButtonProps (onClick, sx, etc.). textTransform:none built in.
```

From src/components/Link.tsx (default export `Link`):
```typescript
export type LinkProps = { children; onClick?; href?; size?: 'sm' | 'md' };
```

From src/components/FlowLayout.tsx (default export `FlowLayout`):
```typescript
export type FlowLayoutProps = { children; maxWidth?: number; px?: number; py?: number };
// px/py are MUI theme-spacing units (8px each). Defaults maxWidth=440, px=6, py=6.
```

From src/components/FetchLogo.tsx (default export `FetchLogo`):
```typescript
export type FetchLogoProps = { size?: number; color?: string; title?: string; tagline?: boolean };
// Default renders the cluster (logo + "CONNECT · SYNC · SIMPLIFY" tagline).
```
</interfaces>

<existing_connect_method_notes>
Current `/connect-method/page.tsx` (read it) already has:
- `handleSelfClick`: `authMethod === 'redirect'` → `router.push(`/connecting?provider=${slug}`)`; else `setOpen(true)`.
- `handleConnect`: pushes `/connecting?provider=${slug}` with `&2fa=1` suffix only when `authMethod === 'credentials'`.
- A single type-aware `Dialog` (DS-styled via `slotProps.paper`) with an `isSftp` flag, host (sftp only) + username + password Inputs, a "Connect" Button, and a "Cancel" Link.
- The `tokens` import from `@/theme/theme` is already present (used for the dialog paper radius).
Principal (credentials) flow is correct and MUST be left functionally intact (only its modal copy may be touched if needed — see T3c; the spec says no change required).
</existing_connect_method_notes>

<lint_tokens_notes>
Current script (package.json `scripts.lint:tokens`), exact:
```
! { grep -rEn '#[0-9a-fA-F]{3,8}' src --include='*.ts' --include='*.tsx' | grep -vE 'src/theme/theme\.ts|src/lib/providers\.ts'; grep -rEn "'[0-9]+px'" src --include='*.ts' --include='*.tsx' | grep -v 'src/theme/theme\.ts'; } | grep . && echo 'lint:tokens PASS — no off-token hex or raw px in src/'
```
T2 appends `src/app/gusto-login` ONLY to the FIRST grep's `grep -vE` alternation (the hex check), making it: `grep -vE 'src/theme/theme\.ts|src/lib/providers\.ts|src/app/gusto-login'`. The px (`'Npx'`) check is NOT modified — instead the Gusto mock must avoid `'Npx'` string literals entirely (use numeric `sx` values for spacing/size, e.g. `borderRadius: 3`, `px: 4`, `fontSize: 28`; numerics serialize to px via MUI and are NOT matched by the `'[0-9]+px'` literal grep). This keeps the px gate untouched and still green.
</lint_tokens_notes>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 (DEMO-01): Rewrite `/` into the demo home launcher</name>
  <files>src/app/page.tsx</files>
  <behavior>
    - `/` renders a Fetch-branded launcher (NO redirect, NO keyframes/animation).
    - Header: FetchLogo cluster, h5 "Fetch demo", body2 intro "Pick a flow to run through the demo." (text.secondary).
    - Three OptionRows, each navigating on click:
      1. "Sign up — join an existing organization" / "Your work domain already has a Fetch org" → `/sign-up?org=existing`
      2. "Sign up — create a new organization" / "Set up a brand-new Fetch organization" → `/sign-up`
      3. "Connection flow" / "Connect a payroll provider — Gusto, Principal, or SFTP" → `/welcome`
  </behavior>
  <action>
Fully replace `src/app/page.tsx` (per DEMO-01). Remove the `useEffect`/`setTimeout` auto-redirect AND both `keyframes` (`scaleIn`, `breathe`) and their `@emotion/react` import — `/` is now a hub, not a splash.

Keep `'use client'` on line 1. Import `useRouter` from `next/navigation`; `Typography` and `Stack` from `@mui/material/*`; `FlowLayout`, `FetchLogo`, `OptionRow` from `@/components/*`.

Render `<FlowLayout maxWidth={440} px={4} py={4}>` wrapping `<Stack spacing={3}>`. First child: a centered header `<Stack spacing={2} sx={{ alignItems: 'center', textAlign: 'center' }}>` containing `<FetchLogo />` (default cluster), `<Typography variant="h5" component="h1" sx={{ color: 'text.primary' }}>Fetch demo</Typography>`, and `<Typography variant="body2" sx={{ color: 'text.secondary' }}>Pick a flow to run through the demo.</Typography>`. Second child: `<Stack spacing={2}>` of the three OptionRows with the EXACT titles/descriptions from the behavior block and these exact nav targets via `onClick={() => router.push(...)}`: `/sign-up?org=existing`, `/sign-up`, `/welcome`.

Use the en-dash "—" in the two "Sign up —" titles exactly as the spec writes them. No off-token hex/px (this file is NOT excluded from lint:tokens). No console.log. No `any`.
  </action>
  <verify>
    <automated>grep -c "useEffect\|keyframes\|breathe\|setTimeout" src/app/page.tsx | grep -qx 0 && grep -q "Fetch demo" src/app/page.tsx && grep -q "router.push('/sign-up?org=existing')" src/app/page.tsx && grep -q "router.push('/sign-up')" src/app/page.tsx && grep -q "router.push('/welcome')" src/app/page.tsx && echo PASS</automated>
  </verify>
  <done>`/` renders the launcher with 3 OptionRows; no redirect/animation code remains; all three nav targets present; tsc clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 (DEMO-02): Build bespoke `/gusto-login` 2-step mock + update lint:tokens exclusion</name>
  <files>src/app/gusto-login/page.tsx, package.json</files>
  <behavior>
    - NEW `'use client'` page with `step: 'signin' | 'authorize'` local state.
    - Gusto-branded (bespoke hex inline, scoped to this file ONLY — NOT FetchLogo, NOT Fetch DS tokens).
    - Step 1 (sign in): coral "gusto" wordmark, "Sign In" heading, Email + Password fields (each with a right-aligned teal "Forgot …" text link), teal full-width "Continue" → step='authorize', "or" divider, white "Sign in with Google" button → step='authorize', teal footer "Get started · Help center".
    - Step 2 (authorize): "gusto" wordmark, heading "Authorize Fetch to connect to your account?", body about viewing payroll account info, teal full-width "Authorize" → `/connecting?provider=gusto`, centered red "Deny" → `/connect-method?provider=gusto`.
    - package.json `lint:tokens` first grep's `grep -vE` also excludes `src/app/gusto-login`; px gate untouched and still green (no `'Npx'` literals in this file).
  </behavior>
  <action>
Create `src/app/gusto-login/page.tsx` ('use client'). This is the SINGLE bespoke-branding exception in the codebase (per DEMO-02). Use MUI `Box`/`Stack`/`Typography`/`TextField` (or plain controlled inputs) for layout; `useState<'signin' | 'authorize'>('signin')` for the step; `useRouter` for navigation. Header is the Gusto wordmark, NOT FetchLogo. The page reads no query params (it's the Gusto-specific mock).

Gusto palette — declare as a small module-scope `const GUSTO = { coral: '#F45D48', teal: '#1C7C6E', tealHover: '#16685C', ink: '#1E0E35', muted: '#6B7281', border: '#D8DEE4', deny: '#C0392B', bg: '#FFFFFF' }` and use these hex values INLINE in `sx`. These hex are allowed here because T2 excludes this file from the lint:tokens hex check. For ALL spacing/sizing use NUMERIC `sx` values (e.g. `borderRadius: 3`, `p: 4`, `fontSize: 28`, `maxWidth: 420`) — do NOT write any `'Npx'` string literal anywhere in this file (the px gate is NOT excluded).

Layout: full-height centered `Box` (white page bg) holding a ~420px white card with a 1px `GUSTO.border` border, rounded corners, subtle shadow, padded.

Step 1 — Sign in (render when step==='signin'):
- Coral lowercase "gusto" wordmark: `<Typography sx={{ color: GUSTO.coral, fontWeight: 700, fontSize: 28 }}>gusto</Typography>`.
- "Sign In" heading (ink, bold).
- Email: a bold "Email" label row with a right-aligned teal "Forgot email?" text link (a Typography/`<Box component="span">` styled teal, clickable no-op or `#`), then a controlled email input (bespoke-styled, teal focus border via GUSTO.teal).
- Password: bold "Password" label with right-aligned teal "Forgot password?", then a controlled password input (type="password").
- Teal full-width "Continue" button (bg GUSTO.teal, hover GUSTO.tealHover, white text) → `setStep('authorize')`.
- "or" divider (a centered muted "or" between two thin `GUSTO.border` rules).
- White "Sign in with Google" button (1px GUSTO.border, ink text, a "G" glyph is fine) → `setStep('authorize')` (demo).
- Footer row: teal "Get started" · "Help center" links.
- Add a short code comment noting the left-side illustration from the source image is intentionally omitted (a centered card is an acceptable mock).

Step 2 — Authorize (render when step==='authorize'):
- "gusto" coral wordmark again.
- Heading "Authorize Fetch to connect to your account?" (ink, bold).
- Body "Authorizing Fetch to connect to your Gusto account will allow Fetch to view and access your payroll account information." (muted).
- Teal full-width "Authorize" button → `router.push('/connecting?provider=gusto')`.
- Centered "Deny" link (GUSTO.deny red) → `router.push('/connect-method?provider=gusto')`. No dead button.

Hold the two controlled input values in `useState` (email, password) so the inputs are controlled; they are not validated (demo). No console.log. No `any` (type the step state and event handlers explicitly).

THEN update `package.json` `scripts.lint:tokens`: in the FIRST grep's `grep -vE` alternation only, append `|src/app/gusto-login` so it reads `grep -vE 'src/theme/theme\.ts|src/lib/providers\.ts|src/app/gusto-login'`. Do NOT modify the second (px) grep. Do NOT touch any other script or dependency.
  </action>
  <verify>
    <automated>test -f src/app/gusto-login/page.tsx && grep -q "Authorize Fetch to connect to your account?" src/app/gusto-login/page.tsx && grep -q "router.push('/connecting?provider=gusto')" src/app/gusto-login/page.tsx && grep -q "router.push('/connect-method?provider=gusto')" src/app/gusto-login/page.tsx && grep -q "src/app/gusto-login" package.json && [ $(grep -c "'[0-9]\+px'" src/app/gusto-login/page.tsx) -eq 0 ] && npm run lint:tokens >/dev/null 2>&1 && echo PASS</automated>
  </verify>
  <done>`/gusto-login` exists with both steps; Authorize/Deny navigate correctly; Gusto hex scoped to this file; lint:tokens excludes it and still PASSES; no `'Npx'` literals in the file.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3 (DEMO-03): Rewire Gusto→/gusto-login and refine the SFTP modal to Figma 8:365</name>
  <files>src/app/connect-method/page.tsx</files>
  <behavior>
    - Gusto (authMethod==='redirect'): `handleSelfClick` now navigates to `/gusto-login` instead of `/connecting?provider=gusto`.
    - SFTP (authMethod==='sftp') modal: title "Sign in to SFTP", subtitle "Enter your SFTP administrator credentials", three Inputs — "Host" (placeholder "Enter the host"), "Username or Email" (placeholder "Enter the username/email"), "Password" type="password" (placeholder "Enter the password"); primary "Sign In" → `/connecting?provider=sftp` (no 2FA); footer caption (text.secondary, centered) "Your credentials are encrypted and never stored by Fetch". DS styling — NO purple #8635f6.
    - Principal (authMethod==='credentials'): unchanged — creds modal → `/connecting?provider=principal&2fa=1` → /verify → /success. Title stays "Sign in to {Provider}".
  </behavior>
  <action>
Edit `src/app/connect-method/page.tsx` (per DEMO-03). Three sub-edits; keep the existing Suspense guard, providers lookup, and credentials path intact.

(3a) In `handleSelfClick`, change the `authMethod === 'redirect'` branch to `router.push('/gusto-login')` (was `router.push(`/connecting?provider=${slug}`)`). Redirect providers are Gusto only, so the static `/gusto-login` target is correct. Update the inline comment accordingly so it no longer claims redirect goes straight to /connecting.

(3b) Refine the SFTP path of the existing type-aware Dialog. The modal is type-aware via `isSftp`. Make the SFTP-mode copy match Figma 8:365 while keeping the navy/accent DS styling (do NOT introduce the Figma mock's purple `#8635f6` or any hex):
  - Title (h5): when `isSftp`, render "Sign in to SFTP" (credentials keeps `Sign in to ${name}`).
  - Subtitle (body2): when `isSftp`, "Enter your SFTP administrator credentials" (credentials keeps its existing read-only note).
  - The Host Input (already rendered only when `isSftp`): set `label="Host"` and `placeholder="Enter the host"`.
  - The username Input: when `isSftp`, `label="Username or Email"` and `placeholder="Enter the username/email"`. (Credentials may keep `label="Username or email"` with no placeholder — do not regress its behavior.)
  - The password Input: keep `type="password"`; when `isSftp`, add `placeholder="Enter the password"`.
  - Primary Button label: when `isSftp`, "Sign In" (credentials path — leave as today, "Connect").
  - Add a footer `<Typography variant="caption" sx={{ color: 'text.secondary', textAlign: 'center' }}>Your credentials are encrypted and never stored by Fetch</Typography>` BELOW the primary button (render for the SFTP path; harmless to show for credentials too — your discretion, but match the spec which scopes it to SFTP). Keep the existing "Cancel" Link.

  `handleConnect` already routes sftp → `/connecting?provider=sftp` (no 2FA suffix) and credentials → `…&2fa=1`; do NOT change `handleConnect`. The "Sign In"/"Connect" label is presentation only.

(3c) Principal/credentials: confirm by reading that the credentials branch still opens the modal and `handleConnect` still appends `&2fa=1`. No code change required for credentials.

No off-token hex/px (this file IS linted). No console.log. No `any`.
  </action>
  <verify>
    <automated>grep -q "router.push('/gusto-login')" src/app/connect-method/page.tsx && grep -q "Sign in to SFTP" src/app/connect-method/page.tsx && grep -q "Enter your SFTP administrator credentials" src/app/connect-method/page.tsx && grep -q "Your credentials are encrypted and never stored by Fetch" src/app/connect-method/page.tsx && grep -q "&2fa=1" src/app/connect-method/page.tsx && ! grep -q "8635f6" src/app/connect-method/page.tsx && echo PASS</automated>
  </verify>
  <done>Gusto self-click → /gusto-login; SFTP modal matches 8:365 copy/fields with DS styling and the encrypted caption; Principal 2FA path intact; no purple hex; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query (`?provider=`) → connect-method render | Untrusted slug; resolved against the catalog only (existing guard), never interpolated into JSX. Unchanged by this plan. |
| Gusto mock inputs → nowhere | Email/password collected on `/gusto-login` are demo-only; never submitted, never stored, never sent anywhere. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information disclosure | `/gusto-login` mock inputs | accept | Demo mock; no network call, no persistence, no real credentials. Inputs are local state only — explicitly out-of-scope per spec (fully mocked, no real OAuth). |
| T-06-02 | Tampering | `/gusto-login` bespoke hex bypassing the token linter | mitigate | Hex is scoped to this one file and the lint:tokens exclusion is narrowed to exactly `src/app/gusto-login` (not a blanket disable); every other src file remains gated. |
| T-06-03 | Spoofing | Gusto-branded mock page | accept | Intentional demo recreation of Gusto's UI for an internal onboarding demo; not deployed as a public credential-harvesting surface. No real Gusto endpoint, no real submit. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — deps unchanged (verified in success criteria). No new packages, so the legitimacy gate is N/A. |
</threat_model>

<verification>
After all three tasks, run the full gate suite from the repo root:

```bash
npx tsc --noEmit
npm run lint
npm run lint:tokens
npm run build
```

All four MUST pass. Then smoke the flow on the dev server (port 3001):
- `/` renders the launcher (no auto-redirect); all 3 OptionRows navigate (`/sign-up?org=existing`, `/sign-up`, `/welcome`).
- Connection flow → `/welcome` → `/permissions` → `/select-provider` (pick Gusto) → `/connect-method` → self → `/gusto-login` → Continue → Authorize → `/connecting?provider=gusto` → `/success`. Deny → `/connect-method?provider=gusto`.
- Pick Principal → self → creds modal → `/connecting?provider=principal&2fa=1` → `/verify` → `/success`.
- Pick SFTP → self → host+creds modal (Host / Username or Email / Password + encrypted caption) → Sign In → `/connecting?provider=sftp` → `/success`.
- Sign-up join & create both reachable from `/`.

Confirm deps unchanged: `git diff --stat package.json package-lock.json` shows only the `lint:tokens` script line changed in package.json (no dependency lines, no lockfile change).
</verification>

<success_criteria>
- [ ] `/` is the demo launcher (no redirect, no keyframes); 3 OptionRows → /sign-up?org=existing, /sign-up, /welcome.
- [ ] `/gusto-login` exists: 2 steps; Authorize → /connecting?provider=gusto → /success; Deny → /connect-method?provider=gusto.
- [ ] Gusto bespoke hex is scoped to `src/app/gusto-login/` and excluded from lint:tokens; px gate untouched and green.
- [ ] connect-method: Gusto self → /gusto-login; SFTP modal matches Figma 8:365 copy/fields in DS styling (no #8635f6); Principal creds→2FA unchanged.
- [ ] tsc / lint / lint:tokens / build all pass.
- [ ] No dead buttons; no new dependencies; no console.log; no `any`.
- [ ] Existing auth/connection routes still work.
</success_criteria>

<output>
Create `.planning/phases/06-demo-home/06-01-SUMMARY.md` when done.
</output>
