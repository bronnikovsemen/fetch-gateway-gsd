---
phase: 05-auth-flows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/sign-in/page.tsx
  - src/app/sign-up/page.tsx
  - src/app/join-organization/page.tsx
  - src/app/create-organization/page.tsx
  - src/app/forgot-password/page.tsx
  - src/app/check-email/page.tsx
  - src/app/set-new-password/page.tsx
  - src/app/password-updated/page.tsx
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]

must_haves:
  truths:
    - "User can open /sign-in, see Email + Password inputs, a right-aligned 'Forgot password?' link, a 'Sign in' button, and a bottom 'Sign up' link"
    - "User can open /sign-up, see Work email + Connection code + Password + Confirm password inputs; 'Create account' goes to /create-organization, and /sign-up?org=existing 'Create account' goes to /join-organization"
    - "User can open /join-organization and /create-organization and reach / via the primary action"
    - "User can walk /forgot-password → /check-email → /set-new-password → /password-updated → /sign-in with no dead buttons"
    - "/password-updated shows the title 'Password updated' in success green with NO checkmark icon (Figma divergence)"
    - "Every one of the 8 routes serves HTTP 200"
  artifacts:
    - path: "src/app/sign-in/page.tsx"
      provides: "AUTH-01 /sign-in screen"
      contains: "Sign in to Fetch"
    - path: "src/app/sign-up/page.tsx"
      provides: "AUTH-01 /sign-up screen with Suspense + useSearchParams ?org branch"
      contains: "useSearchParams"
    - path: "src/app/join-organization/page.tsx"
      provides: "AUTH-02 /join-organization screen"
      contains: "Request to join"
    - path: "src/app/create-organization/page.tsx"
      provides: "AUTH-02 /create-organization screen"
      contains: "Create your organization"
    - path: "src/app/forgot-password/page.tsx"
      provides: "AUTH-03 /forgot-password screen"
      contains: "Reset your password"
    - path: "src/app/check-email/page.tsx"
      provides: "AUTH-03 /check-email screen with demo reset-link"
      contains: "Check your email"
    - path: "src/app/set-new-password/page.tsx"
      provides: "AUTH-03 /set-new-password screen"
      contains: "Set a new password"
    - path: "src/app/password-updated/page.tsx"
      provides: "AUTH-03 /password-updated screen (green title, no icon)"
      contains: "success.main"
  key_links:
    - from: "src/app/sign-up/page.tsx"
      to: "/join-organization and /create-organization"
      via: "useSearchParams('org') === 'existing' branch on Create account"
      pattern: "org.*existing"
    - from: "src/app/check-email/page.tsx"
      to: "/set-new-password"
      via: "demo 'Open the reset link' Link"
      pattern: "set-new-password"
    - from: "src/app/forgot-password/page.tsx"
      to: "/check-email"
      via: "'Send reset link' Button"
      pattern: "check-email"
---

<objective>
Build the 8 net-new auth routes defined in `AUTH-FLOWS-SPEC.md`, assembled entirely from the existing DS components (`FlowLayout`, `FetchLogo`, `Input`, `Button`, `Link`). All screens are mocked — no real auth/session/email. The flow is fully navigable per the spec's navigation graph, with no dead buttons.

Purpose: Deliver the Phase 05 auth slice (AUTH-01/02/03) so a reviewer can walk sign-in, sign-up (both org branches), and the full password-recovery chain end-to-end at 1440px desktop.
Output: 8 `page.tsx` files under `src/app/`, all serving HTTP 200, all gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-auth-flows/AUTH-FLOWS-SPEC.md
@./CLAUDE.md

# The authoritative pattern source. /sign-up's ?org branch mirrors this file's
# Suspense + useSearchParams + content-component shape verbatim.
@src/app/connect-method/page.tsx
# Existing terminal-screen shape (FlowLayout 400 / px=4 py=4 / Stack / FetchLogo 40 / h5 / body2 / DS Button width 100%).
@src/app/success/page.tsx

<interfaces>
<!-- DS component APIs the executor uses directly — no codebase exploration needed. -->

FlowLayout (default import from '@/components/FlowLayout'):
  props: { children, maxWidth?=440, px?=6, py?=6 }
  All 8 screens use <FlowLayout maxWidth={400} px={4} py={4}>.

FetchLogo (default import from '@/components/FetchLogo'):
  props: { size?=40, color?, title? }
  All 8 screens use <FetchLogo size={40} />.

Input (default import from '@/components/Input'):
  props: { label?, value, onChange, placeholder?, type?='text', error?, helperText?, disabled?, fullWidth?=true }
  Controlled — each Input needs its own useState. fullWidth is true by default.

Button (default import from '@/components/Button'):
  props extend MUI Button minus variant/startIcon/size; adds { variant?='primary', size?='md', loading?, iconStart? }
  Primary full-width CTA: <Button variant="primary" sx={{ width: '100%' }} onClick={...}>Label</Button>
  Sentence-case + radius/colors already handled by the wrapper.

Link (default import from '@/components/Link'):
  props: { children, onClick?, href?, size?='md' }
  Auth inline links use size="sm". Navigation via onClick={() => router.push('/target')}.
</interfaces>

<conventions>
<!-- Canonical patterns established across Phases 1-4 (see STATE.md decisions). -->
- Navigation: imperative `'use client'` + `useRouter` from 'next/navigation' + `onClick={() => router.push('/target')}`. No `next/link`.
- Title: <Typography variant="h5" component="h1" sx={{ color: 'text.primary', textAlign: 'center' }}>.
- Subtitle (when present): <Typography variant="body2" sx={{ color: 'text.secondary', textAlign: 'center' }}>.
- Bottom helper text ("Don't have an account?"): <Typography variant="caption" sx={{ color: 'text.secondary' }}>.
- Chrome: <Stack spacing={3} sx={{ alignItems: 'center' }}> inside FlowLayout (matches /success / /connect-method shape).
- Colors/sizes ONLY via theme keys (text.primary, text.secondary, success.main) — zero raw hex / raw px (lint:tokens enforces).
- Apostrophes in copy: use the curly form `’` (e.g. "Don’t", "couldn’t", "We’ll") to match existing screens; React SSR-encodes them.
</conventions>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (AUTH-01): /sign-in and /sign-up (with ?org Suspense branch)</name>
  <files>src/app/sign-in/page.tsx, src/app/sign-up/page.tsx</files>
  <action>
Create two `'use client'` route files using the shared chrome from `<conventions>` (FlowLayout maxWidth=400 px=4 py=4, Stack spacing=3 alignItems center, FetchLogo size 40, h5 title, DS Button full-width, DS Link size="sm").

**/sign-in** (AUTH-01, spec §1) — plain client page (no query param):
- Title "Sign in to Fetch".
- Controlled DS `Input` "Email" (placeholder `you@acme.com`) and DS `Input` "Password" type="password" (separate useState each).
- A right-aligned row holding DS `Link size="sm"` "Forgot password?" → router.push('/forgot-password'). Use a `Box` or `Stack` with `sx={{ width: '100%', display: 'flex', justifyContent: 'flex-end' }}` so the link sits flush right within the 400px panel.
- DS `Button variant="primary" sx={{ width: '100%' }}` "Sign in" → router.push('/') (landing stand-in per spec §"/" landing).
- Bottom centered row (`Stack direction="row" spacing={0.5} sx={{ alignItems: 'center' }}`): `Typography variant="caption"` (text.secondary) "Don’t have an account?" + DS `Link size="sm"` "Sign up" → router.push('/sign-up').

**/sign-up** (AUTH-01, spec §2) — MUST use the Suspense + useSearchParams pattern from `/connect-method`: a default-exported `Page()` returning `<Suspense fallback={null}><SignUpContent /></Suspense>`, with `SignUpContent` calling `useSearchParams()`.
- Title "Create your account".
- Four controlled DS `Input`s in Figma order, each own useState: "Work email" (placeholder `you@acme.com`), "Connection code" (placeholder `025118`), "Password" type="password", "Confirm password" type="password".
- DS `Button variant="primary" sx={{ width: '100%' }}` "Create account" branches on the demo flag: read `const org = searchParams.get('org');` then on click `router.push(org === 'existing' ? '/join-organization' : '/create-organization')`. (Document both URLs in a top comment: `/sign-up` → create-org path; `/sign-up?org=existing` → join-org path.)
- Bottom centered row: `Typography variant="caption"` "Already have an account?" + DS `Link size="sm"` "Sign in" → router.push('/sign-in').

Both files: imports limited to react (useState; Suspense only in /sign-up), next/navigation (useRouter; useSearchParams only in /sign-up), @mui/material/Box|Stack|Typography (layout/text only), and the DS components. No `any`, no console.log, no raw hex/px.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run lint:tokens && grep -q "Sign in to Fetch" src/app/sign-in/page.tsx && grep -q "Create your account" src/app/sign-up/page.tsx && grep -q "useSearchParams" src/app/sign-up/page.tsx && grep -q "org === 'existing'" src/app/sign-up/page.tsx</automated>
  </verify>
  <done>Both files exist; tsc/lint/lint:tokens pass; /sign-in has Email+Password+Forgot-password link+Sign-in CTA+bottom Sign-up link; /sign-up uses Suspense+useSearchParams, has the 4 ordered inputs, and Create-account branches on ?org=existing.</done>
</task>

<task type="auto">
  <name>Task 2 (AUTH-02): /join-organization and /create-organization</name>
  <files>src/app/join-organization/page.tsx, src/app/create-organization/page.tsx</files>
  <action>
Create two `'use client'` route files using the same shared chrome (FlowLayout 400/4/4, Stack spacing 3 alignItems center, FetchLogo 40, h5 title, body2 subtitle, DS Button full-width).

**/join-organization** (AUTH-02, spec §3):
- Title "Join Acme Inc."; subtitle (body2, text.secondary) "An organization already exists for acme.com. Request access to join your team."
- DS `Button variant="primary" sx={{ width: '100%' }}` "Request to join" → router.push('/') (landing stand-in).
- Centered DS `Link size="sm"` "Use a different email" → router.push('/sign-up').
- No inputs on this screen.

**/create-organization** (AUTH-02, spec §4):
- Title "Create your organization"; subtitle (body2, text.secondary) "We couldn’t find an organization for acme.com. Set one up to get started."
- One controlled DS `Input` "Organization name" (placeholder `Acme Inc.`) with its own useState.
- DS `Button variant="primary" sx={{ width: '100%' }}` "Create organization" → router.push('/') (landing stand-in).

Imports limited to react (useState only where an Input exists — /create-organization), next/navigation (useRouter), @mui/material/Stack|Typography, and DS components. No `any`, no console.log, no raw hex/px.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run lint:tokens && grep -q "Request to join" src/app/join-organization/page.tsx && grep -q "Create your organization" src/app/create-organization/page.tsx && grep -q "Organization name" src/app/create-organization/page.tsx</automated>
  </verify>
  <done>Both files exist; tsc/lint/lint:tokens pass; /join-organization has the subtitle + "Request to join" → / + "Use a different email" → /sign-up; /create-organization has the org-name Input + "Create organization" → /.</done>
</task>

<task type="auto">
  <name>Task 3 (AUTH-03): password recovery — 4 routes</name>
  <files>src/app/forgot-password/page.tsx, src/app/check-email/page.tsx, src/app/set-new-password/page.tsx, src/app/password-updated/page.tsx</files>
  <action>
Create four `'use client'` route files using the same shared chrome (FlowLayout 400/4/4, Stack spacing 3 alignItems center, FetchLogo 40, h5 title, body2 subtitle when present, DS Button full-width, DS Link size="sm").

**/forgot-password** (AUTH-03, spec §5):
- Title "Reset your password"; subtitle "Enter your email and we’ll send you a reset link."
- Controlled DS `Input` "Email" (placeholder `you@acme.com`).
- DS `Button variant="primary" sx={{ width: '100%' }}` "Send reset link" → router.push('/check-email').
- Centered DS `Link size="sm"` "Back to sign in" → router.push('/sign-in').

**/check-email** (AUTH-03, spec §6):
- Title "Check your email"; subtitle "We sent a reset link to you@acme.com. It expires in 30 minutes."
- DS `Button variant="primary" sx={{ width: '100%' }}` "Back to sign in" → router.push('/sign-in').
- Centered DS `Link size="sm"` "Resend link" → router.push('/check-email') (re-loads the screen — a real action, not dead).
- PLUS a demo DS `Link size="sm"` "Open the reset link (demo)" → router.push('/set-new-password') (makes the next step click-reachable; task addition, not in Figma).
- No inputs.

**/set-new-password** (AUTH-03, spec §7):
- Title "Set a new password"; subtitle "Choose a new password for your account."
- Two controlled DS `Input`s: "New password" type="password", "Confirm password" type="password" (own useState each).
- DS `Button variant="primary" sx={{ width: '100%' }}` "Update password" → router.push('/password-updated').

**/password-updated** (AUTH-03, spec §8) — FIGMA DIVERGENCE, follow Figma:
- Title rendered in success green with NO checkmark icon: `<Typography variant="h5" component="h1" sx={{ color: 'success.main', textAlign: 'center' }}>Password updated</Typography>`. Do NOT import or render any icon (no CheckCircleRounded). Add a top comment noting the divergence from the task's original CheckCircleRounded request, per "Figma is authoritative."
- Subtitle (body2, text.secondary) "You can now sign in with your new password."
- DS `Button variant="primary" sx={{ width: '100%' }}` "Continue to sign in" → router.push('/sign-in').

Imports limited to react (useState only where Inputs exist — /forgot-password, /set-new-password), next/navigation (useRouter), @mui/material/Stack|Typography, and DS components. No icon imports. No `any`, no console.log, no raw hex/px.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run lint:tokens && grep -q "Reset your password" src/app/forgot-password/page.tsx && grep -q "Check your email" src/app/check-email/page.tsx && grep -q "set-new-password" src/app/check-email/page.tsx && grep -q "Set a new password" src/app/set-new-password/page.tsx && grep -q "success.main" src/app/password-updated/page.tsx && ! grep -q "CheckCircle" src/app/password-updated/page.tsx</automated>
  </verify>
  <done>All four files exist; tsc/lint/lint:tokens pass; /forgot-password → /check-email; /check-email has Resend + demo "Open the reset link" → /set-new-password; /set-new-password → /password-updated; /password-updated title is success.main with NO checkmark icon and "Continue to sign in" → /sign-in.</done>
</task>

</tasks>

<verification>
After all three tasks, run the full gate suite plus a live HTTP smoke of every route and a navigation-graph + dead-button check.

Gates (all must pass):
```
npx tsc --noEmit
npm run lint
npm run lint:tokens
npm run build
```

Live route smoke (dev server on :3001 — reuse a running server or start `npm run dev`; do not change the port). Every route must return HTTP 200:
```
for r in sign-in sign-up join-organization create-organization forgot-password check-email set-new-password password-updated; do
  code=$(curl -s -o /dev/null -w "%{http_code}" "http://localhost:3001/$r")
  echo "$r -> $code"
done
curl -s -o /dev/null -w "sign-up?org=existing -> %{http_code}\n" "http://localhost:3001/sign-up?org=existing"
```

Navigation-graph / no-dead-button check — confirm each documented edge exists in source (every Button/Link has an onClick that navigates or performs a real action):
- /sign-in: Forgot password? → /forgot-password; Sign in → /; Sign up → /sign-up
- /sign-up: Create account → /create-organization (default) or /join-organization (?org=existing); Sign in → /sign-in
- /join-organization: Request to join → /; Use a different email → /sign-up
- /create-organization: Create organization → /
- /forgot-password: Send reset link → /check-email; Back to sign in → /sign-in
- /check-email: Back to sign in → /sign-in; Resend link → /check-email; Open the reset link (demo) → /set-new-password
- /set-new-password: Update password → /password-updated
- /password-updated: Continue to sign in → /sign-in

Constraint checks (all must be 0):
```
grep -rn "from 'next/link'\|from \"next/link\"" src/app/{sign-in,sign-up,join-organization,create-organization,forgot-password,check-email,set-new-password,password-updated}
grep -rn ": any\|<any>\| as any" src/app/{sign-in,sign-up,join-organization,create-organization,forgot-password,check-email,set-new-password,password-updated}
grep -rn "console.log" src/app/{sign-in,sign-up,join-organization,create-organization,forgot-password,check-email,set-new-password,password-updated}
git diff --name-only  # must NOT list any connection-flow route or any file under src/components/
```
Confirm `package.json` dependencies are unchanged (no new deps).
</verification>

<success_criteria>
- All 8 `page.tsx` files exist under `src/app/` and serve HTTP 200 (plus `/sign-up?org=existing` 200).
- `tsc --noEmit`, `lint`, `lint:tokens`, and `build` all pass.
- The full navigation graph from the spec is wired with no dead buttons (every Button/Link navigates or re-loads).
- /sign-up uses Suspense + useSearchParams; "Create account" branches on `?org=existing` (→ /join-organization) vs default (→ /create-organization).
- /password-updated shows a success-green "Password updated" title with NO checkmark icon.
- Zero off-token hex/px; no `any`; no console.log; no `next/link`; no new dependencies.
- No connection-flow routes and no `src/components/*` DS files were modified (net-new routes only).
</success_criteria>

<output>
Create `.planning/phases/05-auth-flows/05-01-SUMMARY.md` when done. In the SUMMARY, flag the /password-updated Figma divergence (green title, no CheckCircleRounded) so it is reviewable, and confirm both /sign-up URLs (default → create-org, ?org=existing → join-org).
</output>
