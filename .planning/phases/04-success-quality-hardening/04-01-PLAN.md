---
phase: 04-success-quality-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/success/page.tsx
autonomous: true
requirements: [FLOW-08]
tags: [mvp, vertical-slice, success, mui-button, mui-icons, client-component, flow-layout-consumer, flow-08]

must_haves:
  truths:
    - "A user landing on /success sees a Fetch-branded centered white panel with a green checkmark, the heading 'Connected successfully', short confirmation body copy, and a 'Done' button"
    - "Clicking 'Done' navigates the user back to /"
    - "The green checkmark uses the success token #10B981 (sourced via theme.palette.success.main, not a raw hex literal in screen code)"
    - "The Phase 1 /success placeholder strings ('Success (`/success`)' and 'Phase 1 placeholder — confirmation panel lands in Phase 4 (FLOW-08).') no longer appear in /success/page.tsx"
    - "End-to-end demo from / → /welcome → /permissions → /select-provider → /connecting?provider=gusto → /success → / completes without dead buttons, errors, or placeholder content"
  artifacts:
    - path: "src/app/success/page.tsx"
      provides: "Real /success confirmation screen — Client Component with FetchLogo + green checkmark icon + 'Connected successfully' heading + confirmation body + 'Done' button → /"
      min_lines: 30
      contains_strings:
        - "'use client'"
        - "Connected successfully"
        - ">Done<"
  key_links:
    - from: "src/app/success/page.tsx"
      to: "/"
      via: "router.push in onClick on the Done Button"
      pattern: "router\\.push\\(['\"]/['\"]"
    - from: "src/app/success/page.tsx"
      to: "@mui/icons-material/CheckCircleRounded"
      via: "default import of CheckCircleRoundedIcon"
      pattern: "from ['\"]@mui/icons-material/CheckCircleRounded['\"]"
    - from: "src/app/success/page.tsx"
      to: "theme.palette.success.main (#10B981)"
      via: "color='success' or sx.color: 'success.main' on the icon"
      pattern: "success\\.main|color=['\"]success['\"]"
---

<objective>
Replace the Phase 1 `/success` placeholder with the real Fetch-branded confirmation screen — a centered white panel containing the Fetch logo, a green checkmark icon using the `#10B981` success token, the heading "Connected successfully", a short confirmation body, and a "Done" button that navigates back to `/`. This is the closer of the five-step demo flow and the last screen-level requirement in v1 (FLOW-08).

**Purpose:** With this plan landed, the user can complete the full demo loop from splash to success and back to splash without hitting any placeholder content or dead buttons.

**Output:** `src/app/success/page.tsx` rewritten as a Client Component with the spec's chrome, copy, icon, and navigation.
</objective>

## Phase Goal

**As a** payroll admin completing the Plantegrity → Fetch onboarding demo, **I want to** see an on-brand confirmation that my (mock) connection succeeded and click a single button to return to the start of the flow, **so that** the demo loop is closed and I can replay it as many times as I want without dead buttons or placeholder screens.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@CLAUDE.md

<!-- Pattern references — the executor should read these to mirror the canonical shape, NOT reinvent it. -->
<!-- The /welcome plan (02-03) is the closest analog: Client Component + FlowLayout + FetchLogo + heading + body + single primary Button with imperative router.push. -->
@.planning/phases/02-pre-provider-flow/02-03-SUMMARY.md
@.planning/phases/03-provider-selection-connecting-bridge/03-02-SUMMARY.md

<interfaces>
<!-- Key contracts extracted from the codebase. Use these directly; no exploration needed. -->

From src/components/FlowLayout.tsx (default + named export):
- FlowLayout component with props { children: ReactNode; maxWidth?: number; px?: number; py?: number }
- Defaults: maxWidth=440, px=6 (= 48px), py=6 (= 48px). The /success panel uses the defaults; pass maxWidth={440} explicitly for readability (matches /welcome and /connecting).
- Page background #EBF5FF comes from FlowLayout itself (bgcolor: 'background.default').

From src/components/FetchLogo.tsx (default + named export):
- FetchLogo component with props { size?: number; color?: string; title?: string }
- Default size = 100. Use <FetchLogo size={100} /> (matches /welcome, /permissions, /connecting).

From src/theme/theme.ts:
- theme.palette.success.main = '#10B981' (the spec's green checkmark token, FOUND-03)
- theme.palette.primary.main = '#2463EC' (Done button color)
- No hex literals should appear in screen code; consume via theme tokens ('success.main', color="primary", etc.)

From @mui/icons-material:
- Use CheckCircleRoundedIcon (default import from '@mui/icons-material/CheckCircleRounded'). It is the closest visual match for the spec's "green checkmark" — a filled circle with a checkmark inside. CheckCircleOutlineRoundedIcon is also acceptable if a thinner stroke is preferred; pick CheckCircleRoundedIcon for the bolder filled look that reads as a success confirmation.

Canonical navigation pattern (from Plan 02-03 /welcome and Plan 02-04 /permissions):
- 'use client' on line 1
- const router = useRouter() from 'next/navigation'
- Button onClick={() => router.push('/')} for the Done CTA
- Button uses variant="contained" color="primary" size="large" with sx={{ textTransform: 'none', fontWeight: 600, alignSelf: 'stretch' }} (sentence-case label per project convention)
- alignItems on Stack lives in sx (the codebase-wide MUI v9.0.1 workaround) — sx={{ alignItems: 'center' }}, NOT alignItems="center" as a top-level prop

Phase 1 placeholder strings currently in src/app/success/page.tsx (must be removed):
- "Success (\`/success\`)"
- "Phase 1 placeholder — confirmation panel lands in Phase 4 (FLOW-08)."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite /success as the real confirmation screen — green checkmark + 'Connected successfully' heading + Done button → /</name>
  <files>src/app/success/page.tsx</files>
  <read_first>
    - src/app/success/page.tsx (the Phase 1 stub being replaced — read to confirm the exact placeholder strings that must disappear)
    - src/app/welcome/page.tsx (closest analog: Client Component + FlowLayout + FetchLogo + heading + body + single primary Button → imperative router.push; mirror its shape and prop conventions)
    - src/app/connecting/page.tsx (recent pattern: Client Component + FlowLayout + Stack with sx={{ alignItems: 'center' }} + heading + body — mirror the alignItems-in-sx convention)
    - src/components/FlowLayout.tsx (props API; the /success panel uses the defaults — maxWidth={440} explicit, no px/py override)
    - src/components/FetchLogo.tsx (props API; use size={100})
    - src/theme/theme.ts (confirms `theme.palette.success.main = '#10B981'` so the icon's color comes from the token, not a hex literal in screen code)
    - CLAUDE.md (locked constraints: MUI-only, FetchLogo is the only icon exception — the checkmark MUST come from `@mui/icons-material`, sentence-case Button labels via `textTransform: 'none'`, port 3001, no `any`, no `console.log`)
  </read_first>
  <action>
    Replace the entire contents of `src/app/success/page.tsx` with a Client Component implementing the FLOW-08 confirmation screen, per FLOW-08 (the sole D-XX-equivalent locked requirement for this plan).

    Structure (in this exact order):
    1. Line 1: `'use client'` directive (the Done button uses imperative navigation via `useRouter`, same pattern as /welcome and /permissions).
    2. Imports (deep imports for MUI components, the same shape used across /welcome, /permissions, /connecting):
       - `useRouter` from `'next/navigation'`
       - `Stack` from `'@mui/material/Stack'`
       - `Typography` from `'@mui/material/Typography'`
       - `Button` from `'@mui/material/Button'`
       - `CheckCircleRoundedIcon` as the default import from `'@mui/icons-material/CheckCircleRounded'` (the icon library is `@mui/icons-material` per CLAUDE.md; this is the one place where a `@mui/icons-material` icon IS the right answer — FetchLogo's "icon library is forbidden" rule applies to the logo, not to in-content iconography)
       - `FlowLayout` from `'@/components/FlowLayout'` (default import)
       - `FetchLogo` from `'@/components/FetchLogo'` (default import)
    3. Default-exported `Page` function component (no props).
    4. Inside the component:
       - `const router = useRouter();`
       - Return JSX rooted at `<FlowLayout maxWidth={440}>` (no `px`/`py` override — relies on the defaults `px=6 py=6` = 48px uniform, matching /welcome's chrome and the spec's /success panel; this is the THIRD defaults-only consumer after /select-provider and /connecting, FIFTH consumer overall).
       - Inside FlowLayout: a single `<Stack spacing={3} sx={{ alignItems: 'center' }}>` (alignItems in `sx`, NOT as a top-level prop — the codebase-wide MUI v9.0.1 workaround).
       - Stack children in this exact order:
         (a) `<FetchLogo size={100} />`
         (b) `<CheckCircleRoundedIcon sx={{ fontSize: 64, color: 'success.main' }} />` — the green checkmark. `color: 'success.main'` sources the `#10B981` success token from the theme (FLOW-08 says "using success token #10B981"). The `fontSize: 64` makes the icon a strong visual anchor next to the 100px FetchLogo; do not use a hex literal here.
         (c) Heading: `<Typography variant="h5" component="h1" sx={{ fontWeight: 700, color: 'text.primary', textAlign: 'center' }}>Connected successfully</Typography>` — semantic `h1` with `h5` visual sizing (the same Typography pattern used in /welcome, /permissions, /connecting).
         (d) Body: `<Typography variant="body1" sx={{ color: 'text.secondary', textAlign: 'center' }}>Your payroll connection is ready. You can now close this window or return to start.</Typography>` — this is the "short confirmation body copy" the spec asks for. Keep it under ~120 characters; it must tell the user what just happened and that they can leave or replay the flow.
         (e) Done button: `<Button variant="contained" color="primary" size="large" onClick={() => router.push('/')} sx={{ textTransform: 'none', fontWeight: 600, alignSelf: 'stretch', mt: 2 }}>Done</Button>` — sentence-case label per project convention (matches the "Get Started" / "Back" / "Continue" / "Connect" pattern); `alignSelf: 'stretch'` so the button spans the inner Stack width inside the 440px panel; `mt: 2` for visual separation from the body copy; `router.push('/')` to close the demo loop back to the splash.

    Do NOT:
    - Place any fenced code block inside this action (per planner rules).
    - Use raw `<div>` or any non-MUI layout primitive.
    - Use Tailwind classes, shadcn imports, lucide-react, or class-variance-authority (QUAL-04 — already locked at Phase 1, must not regress here).
    - Use `<Link>` from `next/link` for the Done CTA — imperative `router.push` is the canonical pattern in this codebase (canonicalized by /welcome and used by /permissions for Back/Continue).
    - Use hex literals (`#10B981`, `#2463EC`, `#EBF5FF`) anywhere in this file — sourcing through theme tokens (`success.main`, `color="primary"`, FlowLayout's `bgcolor: 'background.default'`) is the locked codebase convention.
    - Add a useEffect, useState, useRef, useSearchParams, or any other hook — `/success` is a static confirmation screen with no client state and no query params.
    - Add `export const dynamic = 'force-dynamic'` — there is nothing dynamic to opt out of (no useSearchParams).
    - Add a `console.log`, a `: any`, or a `TODO`/`FIXME` comment.

    Hygiene checklist before committing:
    - `npx tsc --noEmit` must exit 0.
    - No `: any` anywhere in the file (including comments).
    - No `console.log` anywhere in the file.
    - The two Phase 1 placeholder strings ("Success (`/success`)" and "Phase 1 placeholder — confirmation panel lands in Phase 4 (FLOW-08).") must be 0 matches in the rewritten file.
  </action>
  <verify>
    <automated>
      # All grep gates run from repo root; counts use `grep -c` on a single file (no `head`-pipes for counts).
      # Note: `grep -c` on comment-bearing files: where a literal MAY also appear in a JSDoc/inline comment,
      # the gate's expected count below is the source-of-truth (NOT necessarily "1").

      # TypeScript clean
      npx tsc --noEmit  # exit 0

      # Client Component opt-in
      head -3 src/app/success/page.tsx | grep -c "'use client'"  # == 1

      # Phase 1 placeholder strings gone
      grep -cF 'Success (`/success`)' src/app/success/page.tsx                                # == 0
      grep -cF 'Phase 1 placeholder — confirmation panel lands in Phase 4 (FLOW-08).' src/app/success/page.tsx  # == 0

      # FLOW-08 heading present once
      grep -cF 'Connected successfully' src/app/success/page.tsx  # == 1

      # Done button label present once
      grep -cF '>Done<' src/app/success/page.tsx  # == 1

      # Done routes back to / (root) — escaped slash to disambiguate
      grep -cE "router\.push\(['\"]/['\"]\)" src/app/success/page.tsx  # == 1

      # Green checkmark from @mui/icons-material/CheckCircleRounded
      grep -cE "from ['\"]@mui/icons-material/CheckCircleRounded['\"]" src/app/success/page.tsx  # == 1
      grep -cE '<CheckCircleRoundedIcon\b' src/app/success/page.tsx                              # == 1

      # Success token sourced via theme (not raw hex)
      grep -cE "color:\s*['\"]success\.main['\"]" src/app/success/page.tsx  # == 1

      # FlowLayout + FetchLogo + Done navigation imports + render
      grep -cE '<FlowLayout[^>]*maxWidth=\{440\}' src/app/success/page.tsx  # == 1
      grep -cE '<FetchLogo[^>]*size=\{100\}' src/app/success/page.tsx       # == 1

      # Primary Done CTA
      grep -cE 'variant="contained"' src/app/success/page.tsx  # == 1
      grep -cE 'color="primary"' src/app/success/page.tsx       # == 1

      # No hex leaks in screen code
      grep -cF '#10B981' src/app/success/page.tsx  # == 0
      grep -cF '#2463EC' src/app/success/page.tsx  # == 0
      grep -cF '#EBF5FF' src/app/success/page.tsx  # == 0

      # No Tailwind/shadcn/lucide/CVA imports
      grep -cE 'tailwind|shadcn|lucide-react|class-variance-authority' src/app/success/page.tsx  # == 0

      # No <Link> from next/link
      grep -cE "from ['\"]next/link['\"]" src/app/success/page.tsx  # == 0

      # Hygiene
      grep -cE 'console\.log' src/app/success/page.tsx  # == 0
      grep -vE '^\s*//' src/app/success/page.tsx | grep -cE '(^|[^a-zA-Z_]): *any( |;|,|>|$)'  # == 0

      # Live HTTP smoke (run `npm run dev` on port 3001 if not already running, then curl)
      curl -s -o /tmp/success.html -w "%{http_code}" http://localhost:3001/success  # == 200
      grep -cF 'Connected successfully' /tmp/success.html  # == 1 (heading reaches SSR markup)
      grep -cF 'Done' /tmp/success.html                     # >= 1 (Done button label reaches SSR markup)
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 with no errors anywhere in the project.
    - Line 1 of `src/app/success/page.tsx` is exactly `'use client'` (including the directive's matching single quotes).
    - The literal string `Success (`/success`)` returns 0 matches in `src/app/success/page.tsx` (Phase 1 placeholder gone).
    - The literal string `Phase 1 placeholder — confirmation panel lands in Phase 4 (FLOW-08).` returns 0 matches in `src/app/success/page.tsx` (Phase 1 placeholder gone).
    - The literal string `Connected successfully` returns exactly 1 match in `src/app/success/page.tsx`.
    - The literal string `>Done<` returns exactly 1 match in `src/app/success/page.tsx` (the Done button label is in source as `<Button …>Done</Button>` with the `>D` and `e<` boundaries forming the literal).
    - `router.push('/')` (with single OR double quotes around the path) appears exactly once in `src/app/success/page.tsx`.
    - `CheckCircleRoundedIcon` is imported from `@mui/icons-material/CheckCircleRounded` (exactly 1 matching import line) and is rendered exactly once in the JSX (`<CheckCircleRoundedIcon …/>`).
    - The icon's color comes from `success.main` (the theme token sourcing FOUND-03's `#10B981`): `color: 'success.main'` (with single OR double quotes) appears exactly once in `src/app/success/page.tsx`.
    - `<FlowLayout maxWidth={440}>` appears exactly once and there is no `px=` or `py=` override on this FlowLayout (defaults consumed).
    - `<FetchLogo size={100} />` appears exactly once.
    - The Done button has `variant="contained"` and `color="primary"` exactly once each.
    - Hex literals `#10B981`, `#2463EC`, `#EBF5FF` return 0 matches each in `src/app/success/page.tsx` (no leaks — theme tokens only).
    - `tailwind|shadcn|lucide-react|class-variance-authority` returns 0 matches in `src/app/success/page.tsx` (QUAL-04 dependency exclusion preserved).
    - `from 'next/link'` (or double-quoted variant) returns 0 matches in `src/app/success/page.tsx` (imperative `router.push` only, matching the codebase convention).
    - `console\.log` returns 0 matches in `src/app/success/page.tsx`.
    - The non-comment-line scan `grep -vE '^\s*//' src/app/success/page.tsx | grep -cE '(^|[^a-zA-Z_]): *any( |;|,|>|$)'` returns 0 (no `: any` types).
    - Live HTTP smoke: with `npm run dev` running on port 3001, `curl -s -o /tmp/success.html -w "%{http_code}" http://localhost:3001/success` prints `200`, `grep -cF 'Connected successfully' /tmp/success.html` returns `1`, and `grep -cF 'Done' /tmp/success.html` returns at least `1`.
    - End-to-end demo navigability (manual chain check after the live smoke): `/` auto-redirects to `/welcome` (Plan 02-02), Get Started → `/permissions` (Plan 02-03), Continue → `/select-provider` (Plan 02-04), Connect with `gusto` selected → `/connecting?provider=gusto` (Plan 03-01), 2500ms auto-advance → `/success` (Plan 03-02), Done → `/` (this plan). No screen along the way shows placeholder text. Recorded as confirmation note in the SUMMARY; not a separate automated gate (each upstream plan already verified its own segment).
  </acceptance_criteria>
  <done>
    `src/app/success/page.tsx` is a Client Component that renders the FLOW-08 confirmation screen: a centered white panel (FlowLayout maxWidth 440, default 48px uniform padding) with FetchLogo 100px, a green CheckCircleRoundedIcon sized 64px colored via the `success.main` theme token (= `#10B981`), the heading "Connected successfully", short confirmation body copy, and a primary "Done" button (`variant="contained" color="primary"`, sentence case via `textTransform: 'none'`, stretched to the Stack width) that `router.push`es to `/`. All grep gates and the live HTTP smoke pass; `tsc --noEmit` exits 0; no hex leaks; no `console.log`; no `: any`; Phase 1 placeholder strings are gone. FLOW-08 closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /success route (browser to Next.js App Router server) | N/A — `/success` is a static client confirmation screen. No query params, no user input, no API surface, no persistence, no external IO. The only "input" is the URL path itself, which is a fixed literal. |
| Done button onClick → router.push('/') | The navigation target is a static string literal embedded in source. There is no user-controlled data feeding the navigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | `router.push('/')` target in `src/app/success/page.tsx` | accept | The navigation target is a hardcoded string literal `'/'`. No untrusted user input feeds the navigation; tampering would require source-code modification (out of scope for an in-browser threat model). Same posture as Plan 02-03's `router.push('/permissions')` and Plan 02-04's Back/Continue. |
| T-04-01-02 | Information disclosure | Confirmation body copy on `/success` | accept | The "Your payroll connection is ready…" body is spec-mandated public copy. No PII, no secrets, no provider-specific data — generic confirmation that the demo flow completed. |

**No package installs in this task.** No new dependencies introduced. `@mui/icons-material` is already pinned in `package.json` from Plan 01-01 (locked dependency). No legitimacy gate required.

**Phase 4 security surface is minimal by design:** `/success` is a static client-only confirmation screen with one imperative navigation to a literal route. No new trust boundary versus the rest of the demo flow. All STRIDE categories not enumerated above (Spoofing, Repudiation, DoS, Elevation of Privilege) are N/A because there is no auth, no API, no user input, no persistence on this surface.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 across the whole project.
- All grep gates in `<verify><automated>` return their expected counts.
- Live HTTP smoke: `curl http://localhost:3001/success` returns 200 with "Connected successfully" and "Done" in the SSR markup.
- Manual end-to-end demo loop (`/` → /welcome → /permissions → /select-provider → /connecting?provider=gusto → /success → /`) completes without dead buttons, errors, or placeholder content (recorded as confirmation note in the SUMMARY; Plan 04-02 enforces the codebase-wide no-dead-buttons / no-placeholder grep gates).
</verification>

<success_criteria>
- `src/app/success/page.tsx` is rewritten as the real FLOW-08 confirmation screen with FetchLogo + green checkmark (success.main token) + "Connected successfully" + body copy + Done → `/`.
- The Phase 1 placeholder strings no longer appear in the file.
- TypeScript compiles cleanly (`tsc --noEmit` exit 0); no `: any`, no `console.log`, no hex leaks, no forbidden dependency imports.
- Live HTTP smoke confirms heading + Done button reach SSR markup.
- FLOW-08 requirement is closed (the last remaining FLOW requirement in v1).
</success_criteria>

<output>
Create `.planning/phases/04-success-quality-hardening/04-01-SUMMARY.md` when done, following the template at @$HOME/.claude/get-shit-done/templates/summary.md.
</output>
