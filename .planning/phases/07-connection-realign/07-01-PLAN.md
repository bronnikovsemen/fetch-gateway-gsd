---
phase: 07-connection-realign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Input.tsx
  - src/components/PermissionItem.tsx
  - src/app/welcome/page.tsx
  - src/app/permissions/page.tsx
  - src/app/select-provider/page.tsx
  - src/app/connect-method/page.tsx
  - src/app/connecting/page.tsx
  - src/app/verify/page.tsx
  - src/app/success/page.tsx
  - src/app/invite/page.tsx
  - src/app/invitation-sent/page.tsx
  - src/app/recipient/page.tsx
  - src/app/page.tsx
autonomous: true
requirements: [REALIGN-01, REALIGN-02, REALIGN-03]

must_haves:
  truths:
    # REALIGN-01 (T1)
    - "DS Input supports a `select` mode (dropdown) while existing text usages render unchanged"
    - "PermissionItem renders a light-purple circle containing a purple check (CheckRounded), not a bare CheckCircle"
    - "/welcome h5 reads 'Connect your payroll data' with the read-only subtitle and a 'Get Started' button → /permissions"
    - "/permissions card is 640px wide with 40px padding, h5 'Fetch will need access to:', 6 check-circle PermissionItems in a 2-col grid, and a Back + Continue actions row"
    - "/select-provider uses the DS Input dropdown (label 'Connection', chevron) instead of the tonal grey MUI Select, h5 'What do you want to connect?', Back + Continue (disabled until a provider is chosen)"
    # REALIGN-02 (T2)
    - "/connect-method chrome matches the node; per-type self routing (handleSelfClick/handleConnect/modal) is byte-for-byte unchanged"
    - "/connecting shows the spinner, h5 'Establishing connection…', subtitle 'Connecting to {Provider}. You'll be redirected to sign in.', card 400, and keeps the ?2fa=1 gate"
    - "/verify OTP cells are 48×56 with the active cell rendered 2px primary.main (purple), not navy"
    - "/success success mark is a 56px success.main circle containing a white CheckRounded"
    # REALIGN-03 (T3)
    - "/invite has a Back + Send invite actions row (no full-width-button + Back-link)"
    - "/invitation-sent has no Pending Chip and no check mark; the demo 'open as teammate' link still reaches /recipient"
    - "/recipient has no caption footer"
    - "the home '/' Connection-flow OptionRow description says 'payroll system' not 'payroll provider'"
  artifacts:
    - path: "src/components/Input.tsx"
      provides: "DS Input with backward-compatible `select` mode (chevron + children passthrough)"
      contains: "select"
    - path: "src/components/PermissionItem.tsx"
      provides: "check-circle icon (alpha circle + CheckRounded)"
      contains: "CheckRounded"
    - path: "src/app/welcome/page.tsx"
      provides: "/welcome aligned to 454:70"
      contains: "Connect your payroll data"
    - path: "src/app/permissions/page.tsx"
      provides: "/permissions aligned to 456:114 (640 + check-circle + 2-col + actions row)"
      contains: "Fetch will need access to:"
    - path: "src/app/select-provider/page.tsx"
      provides: "/select-provider aligned to 454:82 (DS Input dropdown)"
      contains: "What do you want to connect?"
    - path: "src/app/connecting/page.tsx"
      provides: "/connecting aligned to 454:103 (card 400, h5 not 700-weight)"
      contains: "Establishing connection"
    - path: "src/app/verify/page.tsx"
      provides: "/verify aligned to 455:89 (48×56 cells, purple active cell)"
      contains: "primary.main"
    - path: "src/app/success/page.tsx"
      provides: "/success aligned to 455:115 (success.main circle + white CheckRounded)"
      contains: "CheckRounded"
    - path: "src/app/invite/page.tsx"
      provides: "/invite aligned to 456:147 (Back + Send actions row)"
      contains: "Send invite"
    - path: "src/app/invitation-sent/page.tsx"
      provides: "/invitation-sent aligned to 456:176 (no chip, no mark)"
      contains: "Invitation sent"
    - path: "src/app/recipient/page.tsx"
      provides: "/recipient aligned to 455:129 (no caption)"
      contains: "Plantegrity asked you to connect"
    - path: "src/app/page.tsx"
      provides: "home Connection-flow description copy fix"
      contains: "payroll system"
  key_links:
    - from: "src/app/select-provider/page.tsx"
      to: "src/components/Input.tsx (select mode)"
      via: "DS Input dropdown replaces the tonal MUI Select"
      pattern: "Input[^>]*select"
    - from: "src/app/permissions/page.tsx"
      to: "src/components/PermissionItem.tsx"
      via: "6 PermissionItems rendered with the new check-circle icon"
      pattern: "PermissionItem"
    - from: "src/app/connect-method/page.tsx"
      to: "self-path routing (UNCHANGED)"
      via: "handleSelfClick → /gusto-login | modal; handleConnect → /connecting"
      pattern: "handleSelfClick"
---

<objective>
Re-align the 10 connection-flow screens (plus the home `/` description tweak) to the current Figma "Connection flows" section (node 477:453), exactly as reconciled in `REALIGN-SPEC.md`. Two shared DS components change first (`Input` gains a backward-compatible `select` mode; `PermissionItem`'s icon becomes a check-circle), then every screen is updated for copy, card width, and component swaps.

Purpose: bring the shipped connection demo into pixel/copy parity with the live Figma without touching behavior the demo depends on — the per-type self routing on `/connect-method` (Gusto→/gusto-login; Principal creds modal→2FA; SFTP host+creds form) and `/gusto-login` itself stay exactly as-is.

Output: 13 files modified; tsc/lint/lint:tokens/build green; every screen matches its node; per-type self flows still navigable; no dead buttons; no new deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-connection-realign/REALIGN-SPEC.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@./CLAUDE.md

<constraints>
- MUI v9 (`@mui/material` + `@mui/icons-material` ^9.0.1) + Emotion ONLY. No new deps — `package.json` dependencies must be unchanged.
- Strict TS, no `any`. No `console.log`. Dev/build on port 3001.
- Theme/tokens only: NO off-token hex, NO `'Npx'` string literals in `src/` (outside `src/theme/theme.ts`, `src/lib/providers.ts`, `src/app/gusto-login`). `lint:tokens` must stay green. Source colors via MUI palette keys (`'primary.main'`, `'success.main'`, `'divider'`, `'background.paper'`, `'text.primary'`, etc.); radius via the established `tokens.radius.X / tokens.radius.lg` ratio trick (MUI multiplies a numeric `borderRadius` by `theme.shape.borderRadius` = `tokens.radius.lg`); spacing via `tokens.space[n] / 8` or MUI spacing units. rgba() shadow literals are allowed (they don't trip the linter).
- `alpha` must be imported from `@mui/material/styles` for the PermissionItem tint.
- **CRITICAL — DO NOT TOUCH:** `/gusto-login` (any file under `src/app/gusto-login`), `src/lib/providers.ts` (slugs/names/authMethod), and the per-type routing logic in `src/app/connect-method/page.tsx` — specifically `handleSelfClick`, `handleConnect`, the `authMethod` branch, and the credential `<Dialog>` (modal) markup/state. On `/connect-method` you may ONLY adjust chrome (FlowLayout width/padding, FetchLogo, h5/subtitle copy) IF the node requires it — the spec says it is "already aligned", so verify and change nothing routing-related.
</constraints>

<interfaces>
<!-- Extracted from the codebase. Executor should use these directly — no exploration needed. -->

DS Input — src/components/Input.tsx (current, text-only):
```typescript
export type InputProps = {
  label?: string;
  value: string;
  onChange: (e: React.ChangeEvent<HTMLInputElement>) => void;
  placeholder?: string;
  type?: string;
  error?: boolean;
  helperText?: string;
  disabled?: boolean;
  fullWidth?: boolean;
};
// Wraps MUI <TextField variant="outlined" size="small">. Styling already encodes:
//   white bg, divider border, radius.md (tokens.radius.md / tokens.radius.lg),
//   primary.main focus outline (2px) + notched label, text.disabled idle label.
```

DS Button — src/components/Button.tsx:
```typescript
// variant 'primary' (contained purple) | 'secondary' (tonal navy text). size sm/md/lg.
// loading?: boolean (swaps in CircularProgress). Extends MUI ButtonProps minus variant/startIcon/size.
// Already applies textTransform:'none', fontWeight 600, radius.md.
<Button variant="secondary" onClick={...}>Back</Button>
<Button onClick={...} disabled={...}>Continue</Button>  // primary is the default variant
```

DS Link — src/components/Link.tsx:
```typescript
export type LinkProps = { children: ReactNode; onClick?: () => void; href?: string; size?: 'sm' | 'md'; };
// navy (secondary.main), underline-on-hover. size 'sm' = code 13/20.
```

OptionRow — src/components/OptionRow.tsx:
```typescript
export type OptionRowProps = { title: string; description?: string; selected?: boolean; disabled?: boolean; onClick?: () => void; };
```

FlowLayout — src/components/FlowLayout.tsx:
```typescript
export type FlowLayoutProps = { children: ReactNode; maxWidth?: number; px?: number; py?: number; };
// maxWidth default 440. px/py are MUI spacing units (8px/unit), defaults 6/6 = 48px uniform.
// Outer Box owns page bg + centering; inner Paper owns radius.lg + soft shadow.
```

providers — src/lib/providers.ts (DO NOT MODIFY):
```typescript
export type Provider = { slug: 'gusto' | 'principal' | 'sftp'; name: string; authMethod: 'redirect' | 'credentials' | 'sftp'; };
// default export: readonly [Gusto, Principal, SFTP].
```

theme tokens — src/theme/theme.ts:
```typescript
export const tokens = {
  radius: { xs: 4, sm: 6, md: 8, lg: 12, xl: 16, full: 9999 },
  space: [4, 8, 12, 16, 20, 24, 32, 40, 48, 56, 64, 80, 96],
  text: { body2Medium: {...}, chip: {...}, sectionLabel: {...}, code: {...} },
  status: {...},
};
// MUI palette keys available: primary.main (#635bff purple), secondary.main (#0a2540 navy),
// success.main (#22c55e green), divider (#e2e8f0), background.default/paper, text.primary/secondary/disabled.
```

NOTE on existing screen chrome: `/welcome`, `/permissions`, `/select-provider` currently use a bespoke `<Box>` + `<Paper>` wrapper (NOT FlowLayout) with the `tokens.radius.X / tokens.radius.lg` radius trick and an rgba() shadow. The connection-tail screens (`/connect-method`, `/connecting`, `/verify`, `/success`, `/invite`, `/invitation-sent`, `/recipient`) use `<FlowLayout>`. Preserve whichever wrapper each file already uses unless the spec dictates a width change — change widths via the existing prop (`width:` on the bespoke Paper, `maxWidth=` on FlowLayout), not by rewriting the wrapper.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (REALIGN-01): DS Input select mode + PermissionItem check-circle, and re-align /welcome, /permissions, /select-provider</name>
  <files>src/components/Input.tsx, src/components/PermissionItem.tsx, src/app/welcome/page.tsx, src/app/permissions/page.tsx, src/app/select-provider/page.tsx</files>
  <behavior>
    - Input (text mode, existing usages) still renders an outlined TextField with the same DS styling — no visual/behavioral change for /invite, /connect-method modal, /sign-in, etc.
    - Input with `select` prop renders a TextField in select mode: shows the chosen value, opens a menu of its `children` MenuItems, uses a chevron-down trailing icon, keeps DS styling (white bg, divider border, radius.md, 2px primary.main focus, notched label).
    - PermissionItem renders a ~20px circle tinted `alpha(primary.main, 0.12)` containing a purple CheckRounded (~13px, primary.main) followed by the title (body2Medium, text.primary) and description (caption, text.secondary).
    - /welcome shows h5 "Connect your payroll data" + the read-only subtitle; Get Started → /permissions.
    - /permissions shows a 640px / 40px-padded card, h5 "Fetch will need access to:", 6 check-circle items in 2 columns, and a Back (→/welcome) + Continue (→/select-provider) actions row.
    - /select-provider shows h5 "What do you want to connect?" and a DS Input dropdown labeled "Connection" listing Gusto/Principal/SFTP; Continue is disabled until a provider is chosen.
  </behavior>
  <action>
**1a. DS Input `select` mode (src/components/Input.tsx) — backward-compatible.**
Add two optional fields to `InputProps`: `select?: boolean` and `children?: React.ReactNode`. Pass `select={select}` and `{children}` through to the underlying MUI `<TextField>`. When `select` is true, supply a chevron-down icon via `slotProps={{ select: { IconComponent: KeyboardArrowDownIcon } }}` (import `KeyboardArrowDownIcon` default from `@mui/icons-material/KeyboardArrowDown`). Keep ALL existing `sx` styling (the white-bg / divider-border / radius.md / 2px primary.main focus / notched-label block is unchanged). Default `select = false` and `children = undefined` so EVERY existing text usage is byte-identical in behavior. Do NOT change the `onChange` signature — MUI's select fires `onChange` with the same `ChangeEvent`-shaped event used by `event.target.value`, which the consumer already reads. (If MUI's select typing needs a cast at the boundary, narrow with the existing `React.ChangeEvent<HTMLInputElement>` type already in the signature — do NOT introduce `any`.)

**1b. PermissionItem check-circle (src/components/PermissionItem.tsx).**
Replace the bare `CheckCircleIcon` with a circle-wrapped check, per spec node 462:247. Import `alpha` from `@mui/material/styles` and `CheckRounded` default from `@mui/icons-material/CheckRounded`. Render a 20px square `<Box>` with `borderRadius: tokens.radius.full / tokens.radius.lg` (circle), `bgcolor: (t) => alpha(t.palette.primary.main, 0.12)` (light purple), centered flex, containing `<CheckRounded sx={{ fontSize: 13, color: 'primary.main' }} />`. Keep the rest of the layout (row: icon + title body2Medium text.primary; below: description caption text.secondary). Remove the now-unused `CheckCircleIcon` import.

**1c. /welcome (src/app/welcome/page.tsx) — node 454:70, card 440 (unchanged width).**
Change the h5 to "Connect your payroll data". Change the subtitle to exactly "Plantegrity has requested a secure read-only connection to your payroll data. No data will be modified." Keep the Button label "Get Started" → router.push('/permissions'). Leave the bespoke Box/Paper chrome and 440 width as-is.

**1d. /permissions (src/app/permissions/page.tsx) — node 456:114, card 640, padding 40.**
Set the Paper `width: 640` (was 768). Set padding to 40px uniform via `p: tokens.space[7] / 8` (tokens.space[7] = 40) — replace the current `px: tokens.space[8]/8, py: 4.5`. Change the h5 to "Fetch will need access to:". Update the PERMISSIONS array descriptions to match the spec EXACTLY: Organization "Business profile, contact details, and banking information"; Team "Roster of people and reporting structure"; Employment "Employment status, contact details, role, and compensation"; Payroll "Payments made to employees and contractors"; Pay Statement "Itemized pay statements per employee"; SSN "Social Security Numbers for tax reporting". Keep the column-major 2-col grid; set `columnGap: 4` (gap-32) and `rowGap: 2.5` (gap-20) to match the spec. Keep the actions row: Back (variant secondary, → /welcome) + Continue (primary, → /select-provider). Per spec the Back is fixed ~120px and Continue flex-2: set Back `sx={{ width: 120, flexShrink: 0 }}` and Continue `sx={{ flex: 1 }}` (the secondary Back already exists; just adjust widths). The 6 items now render with the new check-circle icon automatically via 1b.

**1e. /select-provider (src/app/select-provider/page.tsx) — node 454:82, card 440.**
Set the Paper `width: 440` (was 498). Change the h5 to "What do you want to connect?" (subtitle "Choose the system you want to connect" stays). REPLACE the tonal grey MUI `<Select>`/`<MenuItem>` block (and the bold static "Select Payroll Provider" label `<Typography>`, the `action.hover` tonal `sx`, the `renderValue`, the `KeyboardArrowDownIcon`-on-Select, and the `SelectChangeEvent` import + `handleChange`) with the DS `<Input select label="Connection">` from 1a. Its `children` are `<MenuItem value={p.slug}>{p.name}</MenuItem>` for each provider (import `MenuItem` from `@mui/material/MenuItem`). Controlled: `value={selected}`, `onChange={(e) => setSelected(e.target.value as Provider['slug'] | '')}` (keep the existing `useState<Provider['slug'] | ''>('')`). Keep the loading-state submit machinery (`submitting`, `timerRef`, the ~1.2s setTimeout) — or, if simpler and still matching the spec's "Continue (disabled until selected)", you MAY keep it as-is; do NOT remove the disabled-until-selected gate. Actions row: Back (variant secondary, `sx={{ width: 120, flexShrink: 0 }}`, → /permissions) + Continue (primary, `sx={{ flex: 1 }}`, `disabled={!selected}`, → `/connect-method?provider=${selected}`). Remove the now-unused `Select`, `MenuItem`-from-Select, `SelectChangeEvent`, `KeyboardArrowDownIcon` imports that the old tonal implementation pulled in (keep `MenuItem` if you import it fresh for the dropdown children — net: one `MenuItem` import remains).

Verify after each file: no leftover `any`, no `console.log`, no off-token hex / `'Npx'` literal introduced.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint:tokens && grep -c "select" src/components/Input.tsx && grep -c "CheckRounded" src/components/PermissionItem.tsx && grep -c "Connect your payroll data" src/app/welcome/page.tsx && grep -c "Fetch will need access to:" src/app/permissions/page.tsx && grep -c "What do you want to connect?" src/app/select-provider/page.tsx && ! grep -q "import Select from '@mui/material/Select'" src/app/select-provider/page.tsx && echo OK</automated>
  </verify>
  <done>tsc clean; lint:tokens PASS; Input has a backward-compatible `select` mode (text usages unchanged); PermissionItem renders the alpha-circle + CheckRounded; /welcome, /permissions (640 + 40 + 2-col check-circle + Back/Continue), and /select-provider (DS Input dropdown, no tonal Select, Continue disabled until selected) all match their nodes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (REALIGN-02): re-align /connect-method (chrome only), /connecting, /verify (purple cell), /success (green circle)</name>
  <files>src/app/connect-method/page.tsx, src/app/connecting/page.tsx, src/app/verify/page.tsx, src/app/success/page.tsx</files>
  <behavior>
    - /connect-method chrome matches node 452:58; handleSelfClick, handleConnect, the authMethod branch, and the credential Dialog are unchanged — Gusto still → /gusto-login, Principal still → creds modal → /connecting?&2fa=1, SFTP still → host+creds modal → /connecting.
    - /connecting shows the spinner, h5 "Establishing connection…" (NOT 700-weight — uses the theme h5 600), body2 subtitle "Connecting to {Provider}. You'll be redirected to sign in.", card 400; the ?2fa=1 gate (→/verify else →/success) is unchanged.
    - /verify OTP cells are 48 wide × 56 tall; the active cell border is 2px primary.main (purple); filled/empty cells keep their divider borders per spec; "Verify" → /success; "Resend code" clears.
    - /success shows a 56px success.main circle containing a white CheckRounded; h5 "You're connected"; subtitle unchanged; "Continue" → /.
  </behavior>
  <action>
**2a. /connect-method (src/app/connect-method/page.tsx) — node 452:58, card 440 — VERIFY-ONLY for chrome.**
The spec marks this screen "already aligned". Confirm the chrome already matches: FlowLayout maxWidth={440}, FetchLogo cluster, h5 "How do you want to connect {Provider}?", body2 subtitle "Pick whoever has access to your {Provider} account.", two OptionRows ("I'll connect it now" / "I have access to {Provider}"; "Someone on my team manages it" / "We'll send them a secure link to connect"). If all already match the node, make NO change to this file. **DO NOT touch** `handleSelfClick`, `handleConnect`, the `authMethod`/`isSftp` branch, the `<Dialog>` modal, or any of its `useState`. Only if the node demands a copy/spacing tweak may you adjust that exact string/spacing — nothing else. (Expected outcome: zero or near-zero diff here.)

**2b. /connecting (src/app/connecting/page.tsx) — node 454:103, card 400.**
Change `<FlowLayout maxWidth={440}>` to `maxWidth={400}`. Remove the `fontWeight: 700` override on the h5 so it uses the theme h5 (600) — keep `color: 'text.primary'`, `textAlign: 'center'`. Change the subtitle from `variant="body1"` to `variant="body2"` to match the DS subtitle scale; keep the copy verbatim: `Connecting to ${provider.name}. You'll be redirected to sign in.` Optionally drop `size={56}` on FetchLogo to the cluster default (`<FetchLogo />`) for parity with the other tail screens — only if it matches the node. Keep the spinner (`CircularProgress`), and keep the entire ?2fa=1 gate / guard / timer logic UNCHANGED.

**2c. /verify (src/app/verify/page.tsx) — node 455:89, card 400.**
Change each OTP cell from `width: 46, height: 52` to `width: 48, height: 56`. Change the active-cell border color from `secondary.main` (navy) to `primary.main` (purple): the cell `borderColor` becomes `isActive ? 'primary.main' : 'divider'` (keep `border: isActive ? '2px solid' : '1px solid'`). Per spec, filled cells use `bgcolor: 'background.default'` and empty cells `bgcolor: 'background.paper'`; the active cell stays `background.paper`. Implement: `const isFilled = i < code.length;` then `bgcolor: isActive ? 'background.paper' : (isFilled ? 'background.default' : 'background.paper')` (active and empty both paper; filled non-active = default). Keep `borderRadius: tokens.radius.md / tokens.radius.lg`. Keep the hidden capture input, the digit Typography (h5 text.primary), "Verify" → /success, and "Resend code" clearing the code.

**2d. /success (src/app/success/page.tsx) — node 455:115, card 400.**
Replace the bare `<CheckCircleRounded sx={{ color: 'success.main', fontSize: 56 }} />` with a 56px circle containing a white check: a `<Box>` 56×56 with `borderRadius: tokens.radius.full / tokens.radius.lg`, `bgcolor: 'success.main'`, centered flex, containing `<CheckRounded sx={{ fontSize: 28, color: 'background.paper' }} />` (import `CheckRounded` default from `@mui/icons-material/CheckRounded`; import `Box` from `@mui/material/Box`; import `tokens` from `@/theme/theme`; drop the `CheckCircleRounded` import). Keep h5 "You're connected", the subtitle verbatim, and "Continue" → router.push('/').

Verify after each file: no `any`, no `console.log`, no off-token hex / `'Npx'` introduced.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint:tokens && grep -c "handleSelfClick" src/app/connect-method/page.tsx && grep -c "router.push('/gusto-login')" src/app/connect-method/page.tsx && grep -c "maxWidth={400}" src/app/connecting/page.tsx && grep -cE "width: 48" src/app/verify/page.tsx && grep -c "primary.main" src/app/verify/page.tsx && grep -c "CheckRounded" src/app/success/page.tsx && echo OK</automated>
  </verify>
  <done>tsc clean; lint:tokens PASS; /connect-method routing (handleSelfClick → /gusto-login; handleConnect; authMethod branch; Dialog) is byte-for-byte unchanged; /connecting is card 400 with a theme-weight h5 + body2 subtitle and the ?2fa gate intact; /verify cells are 48×56 with a purple (primary.main) active cell; /success shows the success.main circle + white CheckRounded.</done>
</task>

<task type="auto">
  <name>Task 3 (REALIGN-03): re-align /invite (Back+Send row), /invitation-sent (no chip/mark), /recipient (no caption), + home description fix</name>
  <files>src/app/invite/page.tsx, src/app/invitation-sent/page.tsx, src/app/recipient/page.tsx, src/app/page.tsx</files>
  <action>
**3a. /invite (src/app/invite/page.tsx) — node 456:147, card 440.**
Replace the current full-width "Send invite" Button + centered "Back" Link with a horizontal actions row (gap-12 → `spacing={1.5}`): Back (DS `<Button variant="secondary">`, `sx={{ width: 120, flexShrink: 0 }}`, → `router.push('/connect-method?provider=${slug}')`) on the left, Send invite (DS `<Button variant="primary">`, `sx={{ flex: 2 }}`, → `router.push('/invitation-sent?provider=${slug}')`) on the right. Wrap them in `<Stack direction="row" spacing={1.5}>`. Remove the now-unused `Link` import (the Back is now a Button, not a Link). Keep the three controlled Inputs (Work email / Name (optional) / Note (optional)) and all copy unchanged.

**3b. /invitation-sent (src/app/invitation-sent/page.tsx) — node 456:176, card 400.**
Remove the navy check mark (`<CheckCircleRounded ... />`) AND the `<Chip severity="warning" label="Pending" />`. Remove their imports: `CheckCircleRounded` from `@mui/icons-material/CheckCircleRounded` and `Chip` from `@/components/Chip`. Keep h5 "Invitation sent", the subtitle verbatim ("We emailed your teammate a secure link to connect {Provider}. The connection stays pending until they finish."), the "Done" Button → /, and the "Resend invite" Link → /invite. KEEP the demo Link "Open the invite as the teammate (demo)" → `/recipient?provider=${slug}` BELOW "Resend invite" (without it /recipient is unreachable — this is an intentional demo addition, flag it in the SUMMARY as not-in-Figma).

**3c. /recipient (src/app/recipient/page.tsx) — node 455:129, card 400.**
Remove the caption footer `<Typography variant="caption" ...>Continues through the same Permissions → sign-in → Success steps.</Typography>` entirely (it is not in the canonical node). Keep h5 "Plantegrity asked you to connect {Provider}", the subtitle verbatim, and "Get Started" → `/connecting?provider=${slug}&2fa=1` (the self-tail reuse) unchanged.

**3d. home /' (src/app/page.tsx) — Connection-flow OptionRow description.**
Change the third OptionRow's `description` from "Connect a payroll provider — Gusto, Principal, or SFTP" to "Connect a payroll system — Gusto, Principal, or SFTP". Nothing else on this screen changes.

Verify after each file: no `any`, no `console.log`, no off-token hex / `'Npx'` introduced; no leftover unused imports (tsc/lint will catch).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run lint:tokens && grep -c "Send invite" src/app/invite/page.tsx && ! grep -q "Chip" src/app/invitation-sent/page.tsx && ! grep -q "CheckCircleRounded" src/app/invitation-sent/page.tsx && grep -c "Open the invite as the teammate" src/app/invitation-sent/page.tsx && ! grep -q "Continues through the same" src/app/recipient/page.tsx && grep -c "payroll system" src/app/page.tsx && npm run build && echo OK</automated>
  </verify>
  <done>tsc + lint + lint:tokens + build all PASS; /invite has a Back + Send invite actions row (no full-width-button + Back-link); /invitation-sent has no Chip and no check mark, but still links to /recipient (demo); /recipient has no caption footer; the home '/' Connection-flow description reads "payroll system"; no unused imports remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query → screen render | `?provider=` (and `?2fa=1`) cross from the URL into each tail screen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `?provider=` query param on tail screens | mitigate | Unchanged from prior phases — raw `slugParam` is used ONLY as a `providers.find` lookup key; only the trusted catalog `name` is rendered. This re-align does not introduce any new path from `slugParam` to JSX text. Preserve the existing guard/lookup pattern verbatim in every edited screen. |
| T-07-02 | Tampering | DS Input `select` mode (new) | accept | Options are a fixed, hardcoded `providers` list rendered as MenuItem children; user can only pick from the enumerated slugs. No free-text injection surface. |
| T-07-03 | Spoofing/Routing integrity | `/connect-method` self routing | mitigate | The per-type self routing (Gusto→/gusto-login; Principal/SFTP→modal→/connecting) is the demo's core behavior. Mitigation: this plan forbids edits to `handleSelfClick`/`handleConnect`/`authMethod` branch/`<Dialog>`; the Task 2 verify greps assert `handleSelfClick` and `router.push('/gusto-login')` still exist. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — `package.json` dependencies must be unchanged (QUAL-04 / no-new-deps). No legitimacy checkpoint needed because no install task exists. |
</threat_model>

<verification>
Whole-phase gates (run after all 3 tasks):
- `npx tsc --noEmit` exits 0 (strict, no `any`).
- `npm run lint` clean (catches unused imports left over from the Select/Chip/Link/CheckCircleRounded removals).
- `npm run lint:tokens` prints PASS (no off-token hex, no `'Npx'` literal in src/ outside the allowed files).
- `npm run build` succeeds.
- `git diff --stat package.json` shows no dependency change.
- Per-type self routing intact: `grep -c "handleSelfClick" src/app/connect-method/page.tsx` ≥ 1 and `grep -c "router.push('/gusto-login')" src/app/connect-method/page.tsx` ≥ 1; the `<Dialog>` and `authMethod` branch still present.
- `/gusto-login` untouched: `git diff --quiet -- src/app/gusto-login` (no changes).
- Manual node parity spot-check on :3001 (executor or human): /select-provider white "Connection" dropdown + "What do you want to connect?"; /welcome "Connect your payroll data"; /permissions 640 + 2-col check-circle; /verify purple active cell; /invite Back+Send row; /invitation-sent no chip/mark; /recipient no caption; home description "payroll system".
- No dead buttons: every Button/Link has an onClick that lands on a real route.
</verification>

<success_criteria>
- All 13 files updated per REALIGN-SPEC.md; each connection screen matches its 477:453 node.
- DS Input `select` mode is backward-compatible (existing text usages render unchanged); PermissionItem shows the check-circle.
- Per-type self flows still navigable: Gusto→/gusto-login→success; Principal→creds modal→2FA→success; SFTP→host form→success.
- tsc / lint / lint:tokens / build all PASS; no new deps; `/gusto-login` and `providers.ts` untouched; no dead buttons.
</success_criteria>

<output>
Create `.planning/phases/07-connection-realign/07-01-SUMMARY.md` when done. Flag the one intentional non-Figma element: the demo "Open the invite as the teammate" link on /invitation-sent (kept so /recipient stays reachable).
</output>
