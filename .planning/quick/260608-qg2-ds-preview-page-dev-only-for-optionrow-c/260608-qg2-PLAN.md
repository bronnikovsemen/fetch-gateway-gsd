---
phase: quick-260608-qg2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/ds-preview/page.tsx
autonomous: true
requirements: [QG2-PREVIEW]
must_haves:
  truths:
    - "Route /ds-preview renders a single-column page on background.default"
    - "Every DS component variant/state is visible: Button (primary/secondary × sm/md/lg + loading + disabled), OptionRow (4 states), Chip (5 severities × 2 sizes = 10), Input (4 states, controlled), Link (sm + md)"
    - "lint:tokens passes — zero literal hex, zero raw 'Npx' string literals in the new page"
    - "Page is marked dev-only/throwaway in a top comment and is linked from nowhere"
  artifacts:
    - path: "src/app/ds-preview/page.tsx"
      provides: "Standalone DS component preview route"
      contains: "'use client'"
      min_lines: 80
  key_links:
    - from: "src/app/ds-preview/page.tsx"
      to: "@/components/Button, @/components/OptionRow, @/components/Chip, @/components/Input, @/components/Link"
      via: "real component imports (no re-implementation)"
      pattern: "@/components/(Button|OptionRow|Chip|Input|Link)"
---

<objective>
Build ONE throwaway, dev-only preview page so the user can eyeball every DS component variant/state BEFORE wiring them into v2 screens.

Purpose: Visual QA surface for the five DS components that landed in quick task 260608-psx (Button, OptionRow, Chip, Input, Link). Not part of the v1/v2 flow, not linked from anywhere, safe to delete.
Output: `src/app/ds-preview/page.tsx` — route `/ds-preview`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@src/components/Button.tsx
@src/components/OptionRow.tsx
@src/components/Chip.tsx
@src/components/Input.tsx
@src/components/Link.tsx
@src/app/welcome/page.tsx
@src/theme/theme.ts
@package.json

<interfaces>
<!-- Real component contracts the executor consumes directly. Do NOT re-implement. -->

Button — DEFAULT export from '@/components/Button':
  props: { variant?: 'primary' | 'secondary'; size?: 'sm' | 'md' | 'lg'; loading?: boolean; disabled?: boolean; iconStart?: ReactNode; children; ...MuiButtonProps }
  Import as: `import Button from '@/components/Button';`

OptionRow — NAMED export from '@/components/OptionRow':
  props: { title: string; description?: string; selected?: boolean; disabled?: boolean; onClick?: () => void }
  Import as: `import { OptionRow } from '@/components/OptionRow';`

Chip — NAMED export from '@/components/Chip':
  props: { label: string; severity: 'warning' | 'success' | 'rejection' | 'info' | 'neutral'; size?: 'small' | 'medium' }
  Import as: `import { Chip } from '@/components/Chip';`

Input — NAMED export from '@/components/Input' (CONTROLLED):
  props: { label?: string; value: string; onChange: (e: React.ChangeEvent<HTMLInputElement>) => void; placeholder?: string; type?: string; error?: boolean; helperText?: string; disabled?: boolean; fullWidth?: boolean (default true) }
  Import as: `import { Input } from '@/components/Input';`

Link — NAMED export from '@/components/Link':
  props: { children; onClick?: () => void; href?: string; size?: 'sm' | 'md' (default 'md') }
  Import as: `import { Link } from '@/components/Link';`
</interfaces>

<lint_tokens_rules>
<!-- package.json lint:tokens fails on EITHER of two grep matches in src/*.ts(x): -->
<!--   1. an off-token hex literal:  /#[0-9a-fA-F]{3,8}/  -->
<!--   2. a raw px string literal:   /'[0-9]+px'/   (single-quoted, e.g. '440px') -->
<!-- SAFE in this page (NOT flagged): -->
<!--   - bare numeric layout values:  maxWidth: 440, spacing={4}, p: 6, sx={{ mt: 3 }}  -->
<!--   - theme.spacing(N):            maxWidth: theme.spacing(55)  // = 440 -->
<!--   - tokens.space[N] (px ints):   maxWidth: tokens.space[8]    // = 48 etc. -->
<!-- FORBIDDEN in this page: any '#...' hex, any 'Npx' single-quoted string. -->
<!-- For the ~440px OptionRow container use a BARE NUMERIC maxWidth: 440 (simplest, lint-safe). -->
</lint_tokens_rules>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the standalone /ds-preview route with all five DS component sections</name>
  <files>src/app/ds-preview/page.tsx</files>
  <action>
Create `src/app/ds-preview/page.tsx` as a Client Component. Line 1 is `'use client';`. Immediately below, a comment block marking it dev-only/throwaway, e.g. "DEV-ONLY preview page — NOT part of the v1/v2 flow, linked from nowhere, safe to delete. Renders every DS component variant/state for visual QA before v2 wiring."

Imports: `useState` from 'react'; `Box`, `Stack`, `Typography` from `@mui/material/*` (deep imports matching codebase convention); the five DS components per the <interfaces> block above (Button default, OptionRow/Chip/Input/Link named). Do NOT import or re-implement the components inline — use the real `@/components/*` modules.

Page root: a single-column `Box` with `minHeight: '100vh'`, `bgcolor: 'background.default'`, generous padding (e.g. `p: 6`), wrapping one outer `<Stack spacing={6}>`. Add a page `<Typography variant="h4">DS Preview</Typography>` title at top. Each section below is its own `<Stack spacing={2}>` introduced by a `<Typography variant="h5">` section heading. Use bare numeric MUI spacing props / sx units throughout — NEVER literal hex, NEVER '<N>px' single-quoted strings (see <lint_tokens_rules>).

SECTIONS (in this order):

1. Button — heading "Button". Render in a horizontal `<Stack direction="row" spacing={2} sx={{ flexWrap: 'wrap', alignItems: 'center' }}>`:
   - `<Button variant="primary" size="sm">Primary sm</Button>`, same for `md`, `lg`.
   - `<Button variant="secondary" size="sm">Secondary sm</Button>`, same for `md`, `lg`.
   - `<Button variant="primary" loading>Loading</Button>`.
   - `<Button variant="primary" disabled>Disabled</Button>`.
   Each button's children label its variant/size/state.

2. OptionRow — heading "OptionRow". A `<Stack spacing={2}>` of FOUR rows, EACH wrapped in its own container `<Box sx={{ maxWidth: 440 }}>` (bare numeric — mimics screen width; do NOT write '440px'). States:
   (a) `<OptionRow title="Default with description" description="This row has a caption description." />`
   (b) `<OptionRow title="Default without description" />`
   (c) `<OptionRow title="Selected" description="selected=true" selected />`
   (d) `<OptionRow title="Disabled" description="disabled=true" disabled />`
   Below the four rows, a `<Typography variant="caption" sx={{ color: 'text.secondary' }}>hover is live (mouse over)</Typography>`.

3. Chip — heading "Chip". A grid (`<Box sx={{ display: 'grid', gridTemplateColumns: 'repeat(2, max-content)', gap: 2, alignItems: 'center' }}>` or a wrapping row Stack) of ALL 5 severities × BOTH sizes = 10 chips. For each, render `<Chip label="<severity> <size>" severity={severity} size={size} />` so each chip is self-labeling (e.g. label "warning small", "warning medium", … "neutral medium"). Severities: warning, success, rejection, info, neutral. Sizes: small, medium. You may map over a typed const array `const SEVERITIES = ['warning','success','rejection','info','neutral'] as const;` and `const SIZES = ['small','medium'] as const;` — keeps it strict-typed with no `any`.

4. Input — heading "Input". Each Input is CONTROLLED. Declare local state at component top: `const [v1, setV1] = useState('');` … through `v4` (one per Input), or a single state object — your choice, strict-typed. Render a `<Stack spacing={2} sx={{ maxWidth: 440 }}>`:
   (a) `<Input label="Default with label" value={v1} onChange={(e) => setV1(e.target.value)} placeholder="Type here" />`
   (b) `<Input value={v2} onChange={(e) => setV2(e.target.value)} placeholder="No label" />`
   (c) `<Input label="Error field" value={v3} onChange={(e) => setV3(e.target.value)} error helperText="This field has an error" />`
   (d) `<Input label="Disabled" value={v4} onChange={(e) => setV4(e.target.value)} disabled />`
   Below: `<Typography variant="caption" sx={{ color: 'text.secondary' }}>focus is live — click/tab to see navy focus border</Typography>`.

5. Link — heading "Link". A `<Stack direction="row" spacing={3} sx={{ alignItems: 'center' }}>` with `<Link size="sm" href="#">Link sm</Link>` and `<Link size="md" href="#">Link md</Link>` side by side. Below: `<Typography variant="caption" sx={{ color: 'text.secondary' }}>hover underline is live</Typography>`.

Strict TS throughout — no `any`. No `console.log`. No new imports beyond MUI + the five DS components + `useState`. Do NOT add any nav link/entry from other routes; do NOT modify the existing components or any other screen.
  </action>
  <verify>
    <automated>cd /Users/semenbronnikov/Documents/ai-ui-lab/fetch-gateway-gsd && npx tsc --noEmit && npm run lint && npm run lint:tokens && npm run build && git diff --quiet package.json && echo ALL_GATES_PASS</automated>
  </verify>
  <done>
    `src/app/ds-preview/page.tsx` exists with `'use client'` + dev-only top comment. All five sections present with the exact variant/state coverage above (10 chips, 4 OptionRow states each in a maxWidth:440 container, 4 controlled Inputs, primary/secondary × sm/md/lg buttons + loading + disabled, sm+md links). `npx tsc --noEmit`, `npm run lint`, `npm run lint:tokens`, and `npm run build` all pass; `git diff --quiet package.json` exits 0 (no new deps). No other file modified.
  </done>
</task>

</tasks>

<verification>
- Route compiles: `npm run build` includes `/ds-preview` with no errors.
- Token hygiene: `npm run lint:tokens` PASS (no `#hex`, no `'Npx'` in the new page).
- Type safety: `npx tsc --noEmit` exits 0 (no `any`).
- Lint: `npm run lint` clean (no console.log, no unused imports).
- No new deps: `git diff --quiet package.json` exits 0.
- Isolation: `git status` shows ONLY `src/app/ds-preview/page.tsx` added — no existing component or screen modified.
</verification>

<success_criteria>
Visiting `http://localhost:3001/ds-preview` shows a single-column, labeled preview of every DS component variant and state, with live hover/focus interactions, on the brand background — and the page is reachable only by typing the URL (linked from nowhere, safe to delete). All five acceptance gates pass.
</success_criteria>

<output>
Create `.planning/quick/260608-qg2-ds-preview-page-dev-only-for-optionrow-c/260608-qg2-SUMMARY.md` when done.
</output>
