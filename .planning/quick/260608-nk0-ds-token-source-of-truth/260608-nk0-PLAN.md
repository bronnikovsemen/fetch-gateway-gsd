---
phase: quick-260608-nk0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/theme/theme.ts
  - src/components/Button.tsx
  - src/components/FlowLayout.tsx
  - src/components/PermissionItem.tsx
  - src/components/FetchLogo.tsx
  - src/app/page.tsx
  - src/app/welcome/page.tsx
  - src/app/permissions/page.tsx
  - src/app/select-provider/page.tsx
  - src/app/connecting/page.tsx
  - package.json
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
autonomous: true
requirements: [QUAL-05]
must_haves:
  truths:
    - "theme.ts is the ONLY file in src/ containing brand/design hex (providers.ts holds provider brand-color DATA, not design tokens — documented exception)"
    - "Every Fetch palette/typography/radius/spacing value in src/ resolves through the MUI theme or the exported `tokens` object"
    - "`npm run lint:tokens` exits 0 (no off-token hex, no raw px string literal) on the refactored tree"
    - "`npx tsc --noEmit` passes"
    - "Primary renders purple #635bff, page bg near-white #fafafa, success green #22c55e, full type scale applied"
    - "All 5 routes (/, /welcome, /permissions, /select-provider, /connecting) render end-to-end on :3001"
  artifacts:
    - path: "src/theme/theme.ts"
      provides: "Full DS palette + typography scale + `export const tokens = { radius, space }`"
      contains: "export const tokens"
    - path: "package.json"
      provides: "lint:tokens npm script"
      contains: "lint:tokens"
    - path: ".planning/REQUIREMENTS.md"
      provides: "QUAL-05 requirement"
      contains: "QUAL-05"
  key_links:
    - from: "src/components/*, src/app/**"
      to: "src/theme/theme.ts"
      via: "MUI palette keys / theme.spacing() / tokens.radius / tokens.space"
      pattern: "tokens\\.(radius|space)|theme\\.spacing|palette"
---

<objective>
Make the Fetch Design System (Figma "Fetch Design System", key `pZYTXYGKR5lJAcaE0SnzLV`) the single source of truth for design tokens. Rewrite `src/theme/theme.ts` to mirror the DS verbatim, refactor every existing component and route to consume ONLY theme keys / exported tokens, and add a `lint:tokens` enforcement script (requirement QUAL-05) that fails on any off-token hex or raw px literal in `src/` outside `theme.ts`.

Purpose: The current theme follows the obsolete `Main_Fetch_Gateway.md` spec (primary `#2463EC`, bg `#EBF5FF`, success `#10B981`, no type scale) and screens hardcode hex/px directly, so the prototype has drifted from the canonical DS. This task ends the drift and locks it with automation.

Output: A DS-accurate theme, token-only component/route source, and a portable grep-based linter wired into package.json + acceptance notes. The intended VISUAL change (purple primary, near-white page bg, new success green, full type scale) is expected, not a regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@./CLAUDE.md

@src/theme/theme.ts
@src/theme/ThemeRegistry.tsx
@src/components/Button.tsx
@src/components/FlowLayout.tsx
@src/components/PermissionItem.tsx
@src/components/FetchLogo.tsx
@src/app/page.tsx
@src/app/welcome/page.tsx
@src/app/permissions/page.tsx
@src/app/select-provider/page.tsx
@src/app/connecting/page.tsx
@package.json

<canonical_tokens>
Source of truth: Figma "Fetch Design System", file key pZYTXYGKR5lJAcaE0SnzLV. Use verbatim — NO substitutions, NO invented values.

COLORS
  Brand Primary #0a2540 · Brand Accent #635bff
  Text Primary #020810 · Text Secondary #475569 · Text Muted #64748b
  Background Page #fafafa · Background Surface #ffffff · Border Default #e2e8f0
  Warning #f59e0b · Warning Background #fef9c3
  Rejection #ef4444 · Rejection Background #fee2e2
  Success #22c55e · Success Background #f0fdf4
  Info Background #eef2ff · Info Text #4338ca

RADIUS (px): xs 4 · sm 6 · md 8 · lg 12 · xl 16 · full 9999
SPACING (px): 4 8 12 16 20 24 32 40 48 56 64 80 96

TYPOGRAPHY (font / size px / line-height px; Inter — Regular 400, Medium 500, Semi Bold 600)
  h4 SemiBold 28/36 · h5 SemiBold 22/32 · subtitle SemiBold 16/24
  body1 Regular 16/24 · body2 Regular 14/20 · body2-medium Medium 14/20
  button SemiBold 14/20 (textTransform none) · caption Regular 12/16
  overline SemiBold 11/16 (uppercase) · section-label SemiBold 12/16
  chip Medium 13/20 · code Regular 13/20

MAPPING (DS → MUI):
  palette.primary.main   = Brand Accent  #635bff  (DS buttons/links are purple)
  palette.secondary.main = Brand Primary #0a2540  (navy ink)
  background.default     = Background Page    #fafafa
  background.paper       = Background Surface  #ffffff
  divider                = Border Default      #e2e8f0
  text.primary           = Text Primary    #020810
  text.secondary         = Text Secondary  #475569
  text.disabled          = Text Muted      #64748b
  success.main = #22c55e   warning.main = #f59e0b
  error.main   = #ef4444 (Rejection)      info.main = #4338ca (Info Text)
  Also set *.dark / *.light / *.contrastText and the matching "Background" surface tokens
  ONLY where MUI exposes a slot AND a listed hex exists. Do NOT invent shades.
  shape.borderRadius = radius.lg = 12

  DS variant → MUI variant: h4→h4, h5→h5, subtitle→subtitle1, body1→body1,
  body2→body2, button→button, caption→caption, overline→overline.
  Styles with NO 1:1 MUI variant (body2-medium, chip, section-label, code) MUST be
  preserved on the typed `tokens` export — do NOT drop them.
</canonical_tokens>

<linter_boundary>
The `lint:tokens` script enforces "no off-token hex, no raw px" in src/ with these documented exclusions:
  - `src/theme/theme.ts` — the single source of truth; the ONLY file allowed to hold design hex + px primitives.
  - `src/lib/providers.ts` — holds provider BRAND-COLOR DATA (Gusto #F45D48, ADP #D90429, …). These are catalog data, not design tokens, and must stay co-located with provider metadata.
  - SVG geometry: the px RULE targets CSS-style `Npx` STRING literals (e.g. '6px', '48px'), NOT bare numeric SVG attributes (viewBox, path `d`, width/height numbers). FetchLogo's artwork numbers must NOT trip the linter. (Current FetchLogo uses next/image with a raster, so this is forward-proofing — document the boundary regardless.)
  - The hex RULE matches `#[0-9a-fA-F]{3,8}` in any context (comments included). Therefore code comments in refactored files MUST NOT contain hex literals — strip stale `#XXXXXX` references from JSDoc/inline comments while refactoring.
</linter_boundary>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite theme.ts to mirror the Figma DS verbatim + export tokens</name>
  <files>src/theme/theme.ts</files>
  <action>
Replace `src/theme/theme.ts` end to end so it is the single source of truth for every DS token in `<canonical_tokens>`.

1. Update the top-of-file source-of-truth comment to point at the Figma DS — `Fetch Design System` (key `pZYTXYGKR5lJAcaE0SnzLV`) — and REMOVE the `Main_Fetch_Gateway.md` reference. Do NOT write any `#XXXXXX` hex inside the comment block (the linter matches hex in comments too).

2. Define the palette per the MAPPING block: primary.main #635bff, secondary.main #0a2540, background.default #fafafa, background.paper #ffffff, divider #e2e8f0, text.primary #020810 / text.secondary #475569 / text.disabled #64748b, success.main #22c55e, warning.main #f59e0b, error.main #ef4444, info.main #4338ca. Set `*.contrastText` to #ffffff where the surface is dark enough (primary, secondary, success, error, info). Only set `.dark`/`.light` slots when a listed hex covers them; do NOT invent shades — omit the slot otherwise (MUI auto-derives).

3. Set `typography.fontFamily` to the existing `'var(--font-inter), system-ui, sans-serif'` and apply the full type scale: h4 (600, 28px, lineHeight 36/28), h5 (600, 22px, 32/22), subtitle1 (600, 16px, 24/16), body1 (400, 16px, 24/16), body2 (400, 14px, 20/14), button (600, 14px, 20/14, textTransform 'none'), caption (400, 12px, 16/12), overline (600, 11px, 16/11, textTransform 'uppercase'). Use unitless lineHeight ratios (e.g. 36/28) or px strings consistently — px STRINGS in theme.ts are allowed (theme.ts is the linter exception).

4. Set `shape.borderRadius` to 12 (radius.lg).

5. Add a typed tokens export BELOW the theme: `export const tokens = { radius: { xs: 4, sm: 6, md: 8, lg: 12, xl: 16, full: 9999 }, space: [4, 8, 12, 16, 20, 24, 32, 40, 48, 56, 64, 80, 96] } as const;`. Preserve the DS styles with no 1:1 MUI variant by ALSO adding to this export a `text` (or `variants`) sub-object holding `body2Medium` (500/14/20), `chip` (500/13/20), `sectionLabel` (600/12/16, uppercase optional per DS), and `code` (400/13/20) — each `{ fontWeight, fontSize, lineHeight }`. Strict TS, no `any`. `as const` so consumers get literal types.

6. Keep `export default theme;`.
  </action>
  <verify>
    <automated>cd /Users/semenbronnikov/Documents/ai-ui-lab/fetch-gateway-gsd && npx tsc --noEmit && grep -c 'export const tokens' src/theme/theme.ts && grep -q '635bff' src/theme/theme.ts && grep -q '22c55e' src/theme/theme.ts && grep -q 'fafafa' src/theme/theme.ts && grep -q 'pZYTXYGKR5lJAcaE0SnzLV' src/theme/theme.ts && ! grep -q 'Main_Fetch_Gateway' src/theme/theme.ts && echo THEME_OK</automated>
  </verify>
  <done>theme.ts compiles, contains every DS hex verbatim, exports `tokens` (radius + space + non-MUI text styles), points its source-of-truth comment at the Figma DS, and no longer references Main_Fetch_Gateway.md. Type scale applied to h4/h5/subtitle1/body1/body2/button/caption/overline.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor components + routes to consume theme keys / tokens only (incl. FetchLogo color prop)</name>
  <files>src/components/Button.tsx, src/components/FlowLayout.tsx, src/components/PermissionItem.tsx, src/components/FetchLogo.tsx, src/app/page.tsx, src/app/welcome/page.tsx, src/app/permissions/page.tsx, src/app/select-provider/page.tsx, src/app/connecting/page.tsx</files>
  <action>
Remove EVERY hardcoded hex and raw `'Npx'` string literal from these files (in both code AND comments — the linter scans comments). Replace with MUI palette keys, `theme.spacing()`, `tokens.space[...]`, `tokens.radius[...]`, or unitless values. Import `tokens` from `'@/theme/theme'` where you need radius/space primitives that MUI palette/typography don't express.

Per-file specifics (map old → DS via the new theme; the visual change is INTENDED):
- `Button.tsx`: Drop the literal `VARIANT_STYLES` hexes. `primary` → MUI `variant="contained"` driven by `palette.primary` (purple #635bff) with theme-derived hover/disabled (use `sx` referencing `'primary.dark'`/opacity, or let MUI derive); `secondary` → text/tonal variant driven by `palette.secondary`. Replace SIZE_STYLES px strings ('16px'/'24px'/'32px'/'4px'/'8px'/'12px') with `theme.spacing()` units or `tokens.space[...]` (use a callback `sx={(theme) => ({...})}` to reach `theme.spacing`). Replace `borderRadius: '8px'` with `tokens.radius.md`. Keep the public prop API (variant/size/loading/iconStart) unchanged.
- `FlowLayout.tsx`: Replace `borderRadius: '12px'` with `tokens.radius.lg`. The page/paper bgcolors already use `background.default`/`background.paper` tokens — keep. Keep the soft `boxShadow` rgba (rgba() is NOT a hex and NOT a px literal — it does not trip the linter; leave the shadow as-is or move its values into a comment-free local const). Strip the `#EBF5FF`/`#FFFFFF` hex references from the JSDoc comment.
- `PermissionItem.tsx`: Replace `color: '#005EFF'` check icon with `color: 'primary.main'` (now purple per DS); `color: '#001639'` label → `text.primary`; `color: '#6B7281'` description → `text.secondary`. Replace the inline `fontWeight/fontSize/lineHeight` literals by using MUI `variant` props (label → `subtitle`/`body2` medium via `tokens.text.body2Medium`, description → `caption` or `body2`). Strip hex from the comment block.
- `FetchLogo.tsx`: Make `color` flow from the theme instead of being ignored. Keep next/image raster usage, but add a `color` prop that DEFAULTS to a theme palette token (e.g. `'currentColor'` resolved against `secondary.main`, OR accept `color?: string` defaulting to the navy ink token sourced from the theme — do NOT hardcode a hex default). Since the current logo is a raster `<img>` that can't be tinted, satisfy the requirement by: (a) keeping the prop typed and documented as theme-sourced, and (b) ensuring NO hex default appears in the file. If a tint is needed, apply it via an `sx`/`style` `color` that reads from the theme (`secondary.main`) rather than a literal. The hard requirement: zero hardcoded hex in FetchLogo.tsx; `color` default must reference a theme token name, not a hex.
- `welcome/page.tsx`, `permissions/page.tsx`, `select-provider/page.tsx`: Replace every `bgcolor: '#F3FCFF'`/`'#FFFFFF'` with `background.default`/`background.paper` (or `secondary`/`primary` tones where appropriate); every `color: '#001639'` → `text.primary`, `'#6B7281'` → `text.secondary`, `'#005EFF'` borderColor → `primary.main`. Replace inline `fontSize/fontWeight/lineHeight` with MUI `variant` props (headings → `h5`/`subtitle1`, body → `body2`). Replace every px STRING (`'6px'`,`'48px'`,`'36px'`,`'4px'`,`'16px'`) with `theme.spacing()` (use the `sx={(theme)=>(...)}` callback) or `tokens.space[...]` / `tokens.radius[...]`. The `/welcome` and others may keep using `FlowLayout`'s `px`/`py` theme-spacing-unit API where applicable. Strip hex from all comment blocks.
- `page.tsx` (splash) and `connecting/page.tsx`: These already use `background.default`/`text.*` tokens and have no code hex — scan for any px string literals and stray hex IN COMMENTS and remove them. Keep the keyframes (no hex/px there).

CONSTRAINTS: MUI v9 + Emotion only; no Tailwind/shadcn/lucide; strict TS (no `any`); no console.log; keep all routes navigable (no logic/route changes — purely token sourcing). Do NOT touch `src/lib/providers.ts` (provider brand-color data stays).
  </action>
  <verify>
    <automated>cd /Users/semenbronnikov/Documents/ai-ui-lab/fetch-gateway-gsd && npx tsc --noEmit && OFFENDERS=$(grep -rEn '#[0-9a-fA-F]{3,8}' src --include='*.ts' --include='*.tsx' | grep -vE 'src/theme/theme\.ts|src/lib/providers\.ts'; grep -rEn "'[0-9]+px'" src --include='*.ts' --include='*.tsx' | grep -vE 'src/theme/theme\.ts'); if [ -n "$OFFENDERS" ]; then echo "OFFENDERS FOUND:"; echo "$OFFENDERS"; exit 1; fi; echo REFACTOR_CLEAN</automated>
  </verify>
  <done>tsc passes; zero hex literals in src/ outside theme.ts and providers.ts; zero `'Npx'` string literals in src/ outside theme.ts; FetchLogo `color` prop defaults to a theme-token reference (no hex); every brand color/size in the refactored files resolves through the theme or `tokens`. Routes unchanged structurally.</done>
</task>

<task type="auto">
  <name>Task 3: Add lint:tokens enforcement script (QUAL-05) + update .planning docs</name>
  <files>package.json, .planning/REQUIREMENTS.md, .planning/PROJECT.md</files>
  <action>
1. Add a portable `lint:tokens` npm script to `package.json` `scripts`. Use POSIX `grep -rEn` (NOT ripgrep — rg may be absent). It MUST scan only `*.ts`/`*.tsx` under `src/`, EXCLUDE `src/theme/theme.ts` (hex+px exception) and `src/lib/providers.ts` (provider brand-color data exception), and exit non-zero if it finds EITHER (a) a color hex `#[0-9a-fA-F]{3,8}` OR (b) a CSS-style px string literal `'[0-9]+px'`. Implement as a single script string that runs grep, inverts the exit code, and prints offenders. Suggested shape (single line in package.json):
   `"lint:tokens": "! { grep -rEn '#[0-9a-fA-F]{3,8}' src --include='*.ts' --include='*.tsx' | grep -vE 'src/theme/theme\\.ts|src/lib/providers\\.ts'; grep -rEn \"'[0-9]+px'\" src --include='*.ts' --include='*.tsx' | grep -v 'src/theme/theme\\.ts'; } | grep . && echo 'lint:tokens PASS — no off-token hex or raw px in src/'"`
   Adjust quoting so it is valid JSON and runs under `sh`/`zsh`. The script must:
     - PASS (exit 0) when there are zero offenders, printing a PASS line;
     - FAIL (exit non-zero) and print the offending file:line when any hex/px literal appears outside the two excepted files.
   Document the px-vs-SVG boundary in a short comment-free README note OR inline in the package.json adjacent context is not possible — instead capture the boundary in the QUAL-05 requirement text and PROJECT.md decision (see steps 2-3). NOTE: the px rule targets `'Npx'` STRING literals, so bare SVG numeric attributes never match — no FetchLogo false-positive.

2. `.planning/REQUIREMENTS.md`: Add under `### Quality` a new entry:
   `- [x] **QUAL-05**: All design values in src/ trace to theme.ts tokens (the Figma "Fetch Design System", key pZYTXYGKR5lJAcaE0SnzLV); no off-token hex or raw px outside theme.ts (provider brand colors in src/lib/providers.ts are catalog data, not design tokens); enforced by \`npm run lint:tokens\`. The px rule targets CSS-style \`Npx\` string literals, not bare SVG numeric attributes.`
   Add a Traceability row: `| QUAL-05 | Quick 260608-nk0 (DS token source of truth) | Complete |`. Update the coverage line if present (note QUAL-05 as an added quality requirement beyond the v1 22).

3. `.planning/PROJECT.md` Key Decisions table: Add a row —
   `| Figma DS is the single source of truth for design tokens; theme.ts mirrors it; off-token hex/px forbidden in src/ (enforced by \`npm run lint:tokens\`) | Prior theme followed obsolete Main_Fetch_Gateway.md and screens hardcoded hex/px, causing drift from the canonical DS | ✓ Quick 260608-nk0 |`
   Also append a short note (in the Context or a "Visual change" sentence) recording the INTENDED visual change: primary now purple #635bff, page background near-white #fafafa, success green #22c55e, full type scale applied — this is intentional, NOT a regression.
  </action>
  <verify>
    <automated>cd /Users/semenbronnikov/Documents/ai-ui-lab/fetch-gateway-gsd && npm run lint:tokens && grep -q 'QUAL-05' .planning/REQUIREMENTS.md && grep -q 'lint:tokens' package.json && grep -q 'pZYTXYGKR5lJAcaE0SnzLV' .planning/PROJECT.md && echo DOCS_OK</automated>
  </verify>
  <done>`npm run lint:tokens` exists, is portable (grep-based), excludes theme.ts + providers.ts, exits 0 on the clean tree and would exit non-zero on any reintroduced hex/px. QUAL-05 added to REQUIREMENTS.md (entry + traceability). PROJECT.md logs the DS-source-of-truth decision and the intended visual change. The px-vs-SVG boundary is documented in QUAL-05 text.</done>
</task>

</tasks>

<verification>
End-to-end acceptance (run after all 3 tasks):
1. `npx tsc --noEmit` exits 0 (strict TS, no `any`).
2. `npm run lint:tokens` exits 0 — zero off-token hits in src/ excluding theme.ts (+ providers.ts data exception).
3. theme.ts is the only design-token file: `grep -rEn '#[0-9a-fA-F]{3,8}' src --include='*.ts' --include='*.tsx' | grep -vE 'theme\.ts|providers\.ts'` returns nothing.
4. Dev server on :3001 serves HTTP 200 for all 5 routes: `/`, `/welcome`, `/permissions`, `/select-provider`, `/connecting?provider=gusto`. (Start `npm run dev`, curl each, confirm 200 + SSR heading markers; tear down if you started it.)
5. No `console.log` in committed src/. No Tailwind/shadcn/lucide deps in package.json.
6. Visual change present (purple primary, #fafafa bg, #22c55e success, type scale) — intended, recorded in PROJECT.md.
</verification>

<success_criteria>
- src/theme/theme.ts mirrors the Figma DS verbatim (every listed hex + full type scale + `export const tokens`), source-of-truth comment points at the Figma DS, no Main_Fetch_Gateway.md reference.
- Every component + route in src/ consumes theme keys / `tokens` only — no hardcoded hex, no raw px string literal (FetchLogo `color` defaults to a theme token, not a hex).
- `npm run lint:tokens` enforces the rule portably and passes on the refactored tree.
- QUAL-05 recorded in REQUIREMENTS.md; DS-source-of-truth decision + intended visual change logged in PROJECT.md.
- `npx tsc --noEmit` passes; all 5 routes render on :3001.
</success_criteria>

<output>
Create `.planning/quick/260608-nk0-ds-token-source-of-truth/260608-nk0-SUMMARY.md` when done.
</output>
